2026-06-20 — Issue thinlines/ledger-flow#3 (`issues/005-remove-backdrop-filter-add-no-blur-guard.md`)

Removed every `backdrop-filter` / `backdrop-blur-*` from the frontend per ADR-0001
and added a vitest source-guard so the decision cannot regress.

Edits:
- `app/frontend/src/routes/+layout.svelte` — mobile top bar, sidebar brand card,
  sidebar nav sections: `bg-white/{75,82}` + `backdrop-blur-lg` → `bg-white`.
- `app/frontend/src/routes/transactions/+page.svelte` — sticky totals footer:
  `bg-white/90 backdrop-blur-sm` → `bg-white`.
- `app/frontend/src/routes/accounts/[accountId]/reconcile/+page.svelte` — mobile
  footer: `max-shell:bg-white/95 max-shell:backdrop-blur` → `max-shell:bg-white`.
- `app/frontend/src/lib/components/transactions/TransactionDayGroup.svelte` —
  sticky date-header row: dropped `backdrop-filter: saturate(1.2)` and made the
  background fully opaque (`rgb(255,255,255)`). Same per-frame compositing cost
  the issue is fixing.
- New `app/frontend/src/lib/no-backdrop-filter.test.ts` — walks `src/` and asserts
  no source file (other than test files) contains `backdrop-filter` or
  `backdrop-blur-*`. Verified the guard fails when a forbidden token is
  reintroduced and passes when clean.

Verification:
- `pnpm test` → 4 files, 55 tests pass (including the new guard).
- `pnpm check` surfaces one pre-existing error in `transactions/+page.svelte`
  about a missing `ManualResolutionDialog.svelte` import — that file is being
  deleted as part of unrelated in-progress work in the same working tree, not by
  this change. Out of scope for this issue.
- Scope discipline: no edits to body gradient, card gradients, shadows, layout,
  or any backend code. Running-balance / day-group-sum / N-1 collapse logic
  untouched.

Outstanding:
- HITL sign-off (the reporter scrolling the worst-case Transactions filter and a
  short page) — cannot be completed by an agent.
- `gh` is unauthenticated in this environment, so the GitHub issue was not
  commented on or closed via the CLI. Local issue mirror
  (`issues/005-...md`) has been updated with completed acceptance checkboxes and
  the label flipped to `done-pending-hitl`. The user should close
  thinlines/ledger-flow#3 once they've signed off on scroll smoothness.
