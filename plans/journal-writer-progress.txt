2026-06-20 — PR 1 task 1/3 complete: introduced services/journal_writer.py.

  Module: app/backend/services/journal_writer.py
    - mutate(*, config, paths, tag, event_type, verify=None, actor="user") context manager
    - JournalMutation(event_id, summary, payload, compensates) handle
    - VerifyFailure marker base class
    - WriterRejected / WriterUnavailable / WriterError exception surface
    - Inlined backup helper (backup_service.py to be deleted in PR 2)
    - Path validation: at least one *.journal path required

  Tests: app/backend/tests/test_journal_writer.py — 28 tests, all pass.
    Covers: event_id minting, hash_before consistency, backup creation,
    successful-exit event shape and journal_refs filtering (changed paths
    only, newly-created paths included), block-exception rollback (existing
    files restored byte-for-byte, created files deleted, no event emitted),
    verify→VerifyFailure rollback + WriterRejected, verify→RuntimeError
    rollback + WriterUnavailable, zero-journal-path rejection, partial
    rollback aggregation + original-exception preservation via __context__.

  Docs: CONTEXT.md created (lazy per PRD Further Notes) with journal-mutation
  definition + scope.

  Full backend suite: 804 passed (28 new + 776 existing, no regressions).

  Next: convert accounts_reconcile route to use mutate(verify=verify_assertion);
  then convert _undo_transaction_deleted handler.

2026-06-20 — PR 1 task 2/3 complete: converted accounts_reconcile route.

  Route: app/backend/main.py — accounts_reconcile
    - Wrapped journal write in `with journal_writer.mutate(paths=[year journal],
      tag="reconcile", event_type="account.reconciled.v1",
      verify=lambda cfg, _paths: verify_assertion(cfg)) as mut:`
    - Uses `mut.event_id` (writer-minted) to embed the reconciliation_event_id in
      journal metadata via write_assertion_transaction(event_id=mut.event_id),
      preserving DECISIONS §15 byte-identity between the in-journal metadata and
      the emitted event id.
    - Sets `mut.summary` + `mut.payload` inside the block — writer emits the
      event with assembled journal_refs on successful exit.
    - WriterRejected (verify_assertion → AssertionFailure) translates to
      HTTP 422 with outcome/expected/actual/rawError detail preserved.
    - WriterUnavailable (verify_assertion → RuntimeError) translates to
      HTTP 500 with the existing "ledger CLI is unavailable" copy.
    - write_assertion_transaction return tuple now discards `backup_path`
      (writer owns rollback). Inner backup still happens; harmless until PR 2
      sweeps backup_service.

  Adapter: reconciliation_service.AssertionFailure now subclasses
  journal_writer.VerifyFailure (marker). No callers needed changes; the
  inheritance just lets the writer carry it via WriterRejected.failure.

  Imports: main.py drops `restore_from_backup` (no remaining route uses it).
  reconciliation_service.py imports VerifyFailure for the marker base.

  Tests: 70 focused tests pass (test_reconcile_endpoint 13, test_reconciliation
  _service 29, test_journal_writer 28). No shape changes to existing tests.
  Full backend suite: 804 passed, 0 failures.

  Next: convert _undo_transaction_deleted handler in app/backend/services/
  undo_service.py to flow through mutate(..., compensates=original_event_id).

2026-06-20 — PR 1 task 3/3 complete: converted _undo_transaction_deleted handler.

  Handler: app/backend/services/undo_service.py — _undo_transaction_deleted
    - Signature changed from (workspace_path: Path, event: dict) -> dict[str, str]
      to (config: AppConfig, event: dict) -> str (returns the compensating
      event id).
    - Restoration logic wrapped in `with journal_writer.mutate(paths=[journal],
      tag="undo", event_type=f"{forward_type}.compensated.v1") as mut:`.
    - Sets `mut.summary`, `mut.payload` (forward payload + compensated_event_id),
      `mut.compensates = forward_event_id` inside the block — writer emits the
      compensating event with assembled journal_refs on successful exit.
    - Pre-block safety check (refuse if header already present) stays outside
      `mutate` so a duplicate-rejection neither backs up nor emits.
    - Legacy `backup_file(journal_path, "undo")` call dropped — writer owns
      backup + rollback now.

  Dispatcher: undo_event(config: AppConfig, event_id: str)
    - Signature changed from undo_event(workspace_path, event_id) — workspace
      is taken from config.root_dir internally.
    - New HandlerFn protocol union (LegacyHandlerFn | WriterHandlerFn);
      _WRITER_HANDLERS frozenset names event types whose handlers use the
      writer protocol. Discrimination via set lookup, not isinstance.
    - Legacy emit-on-success branch retained for the six remaining handlers
      until PR 2 sweeps them.
    - Per-event drift verification stays in undo_event (precondition before
      handler dispatch), per PRD Decision 5.

  Caller: main.py events_undo route now passes `config` instead of
  `config.root_dir`.

  Tests: app/backend/tests/test_undo_service.py
    - Added _make_config(workspace) helper (minimal AppConfig — root_dir +
      empty dicts).
    - All 16 call sites updated mechanically: undo_event(workspace, id) →
      undo_event(_make_config(workspace), id). No assertion shape changes.
    - 15 tests pass (one is parametrized count). Round-trip restore test
      still asserts journal text restoration; already-compensated test still
      asserts r1.compensating_event_id == r2.compensating_event_id, now
      verifying that the writer-emitted event id is what _is_compensated
      finds.

  Full backend suite: 804 passed, 0 failures.

  PR 1 (proof) is now complete. Next: PR 2 (sweep) — convert remaining ~14
  mutation routes and six undo handlers, delete backup_service.py, remove
  restore_from_backup from the event-log service's public exports, drop
  the legacy-handler branch from undo_event.

2026-06-20 — PR 2 sweep step 1: converted _undo_transaction_recategorized.

  Handler: app/backend/services/undo_service.py — _undo_transaction_recategorized
    - Signature changed from (workspace_path: Path, event: dict) -> dict[str, str]
      to (config: AppConfig, event: dict) -> str (returns the compensating event id).
    - Body wrapped in `with journal_writer.mutate(paths=[journal_path], tag="undo",
      event_type=f"{forward_type}.compensated.v1") as mut:`. Header lookup,
      block lookup, posting rewrite, and journal write all happen inside the
      block — if the destination account is not found the handler raises
      UndoFailedError inside the block and the writer rolls back (no-op since
      the in-memory `lines` mutation never reached disk, but backup is still
      restored harmlessly).
    - Legacy `backup_file(journal_path, "undo")` call dropped — writer owns
      backup + rollback now.
    - Sets `mut.summary` (f"Undid: {forward_summary}"), `mut.payload`
      (forward payload + compensated_event_id), `mut.compensates =
      forward_event_id` inside the block. Writer emits the compensating event
      with assembled journal_refs on successful exit.

  Dispatcher: _WRITER_HANDLERS now contains both transaction.deleted.v1 and
  transaction.recategorized.v1. No further dispatcher changes — the existing
  set-lookup discrimination handles the new entry transparently.

  Tests: test_undo_service.TestUndoRecategorized.test_round_trip_recategorize_
  and_restore passes unchanged. Full undo + journal_writer focused: 43 tests.
  Full backend suite: 804 passed, 0 failures.

  Next: convert _undo_transaction_account_reassigned (structurally identical
  to recategorized — same posting-rewrite pattern).

2026-06-20 — PR 2 sweep step 2: converted _undo_transaction_account_reassigned.

  Handler: app/backend/services/undo_service.py — _undo_transaction_account_reassigned
    - Signature changed from (workspace_path: Path, event: dict) -> dict[str, str]
      to (config: AppConfig, event: dict) -> str.
    - Body wrapped in `with journal_writer.mutate(paths=[journal_path], tag="undo",
      event_type=f"{forward_type}.compensated.v1") as mut:`. Read, header lookup,
      block lookup, posting rewrite, and journal write all happen inside the
      block; UndoFailedError raised inside the block triggers writer rollback.
    - Legacy `backup_file(journal_path, "undo")` call dropped — writer owns
      backup + rollback now.
    - Sets `mut.summary` (f"Undid: {forward_summary}"), `mut.payload`
      (forward payload + compensated_event_id), `mut.compensates =
      forward_event_id` inside the block. Writer emits the compensating event
      with assembled journal_refs on successful exit.

  Dispatcher: _WRITER_HANDLERS now contains transaction.deleted.v1,
  transaction.recategorized.v1, and transaction.account_reassigned.v1. No
  further dispatcher changes — set-lookup discrimination handles the new entry
  transparently.

  Tests: No existing tests directly cover this handler; the dispatcher path
  exercises it via _HANDLERS lookup + writer-routed protocol. Full undo +
  journal_writer focused: 43 tests pass. Full backend suite: 804 passed,
  0 failures.

  Next: convert _undo_transaction_status_toggled.

2026-06-20 — PR 2 sweep step 3: converted _undo_transaction_status_toggled.

  Handler: app/backend/services/undo_service.py — _undo_transaction_status_toggled
    - Signature changed from (workspace_path: Path, event: dict) -> dict[str, str]
      to (config: AppConfig, event: dict) -> str.
    - Body wrapped in `with journal_writer.mutate(paths=[journal_path], tag="undo",
      event_type=f"{forward_type}.compensated.v1") as mut:`. Read, new_status
      validation, current-header reconstruction (set_header_status(header_line,
      new_status)), header locate, previous_status validation, status restore,
      and journal write all happen inside the block. UndoFailedError raised
      inside the block triggers writer rollback.
    - Legacy `backup_file(journal_path, "undo")` call dropped — writer owns
      backup + rollback now.
    - Sets `mut.summary` (f"Undid: {forward_summary}"), `mut.payload`
      (forward payload + compensated_event_id), `mut.compensates =
      forward_event_id` inside the block. Writer emits the compensating event
      with assembled journal_refs on successful exit.

  Dispatcher: _WRITER_HANDLERS now contains transaction.deleted.v1,
  transaction.recategorized.v1, transaction.account_reassigned.v1, and
  transaction.status_toggled.v1. No further dispatcher changes — set-lookup
  discrimination handles the new entry transparently.

  Tests: test_undo_service.TestUndoStatusToggled.test_round_trip_toggle_and_
  restore passes unchanged. Full undo + journal_writer focused: 43 tests pass.
  Full backend suite: 804 passed, 0 failures.

  Next: convert _undo_manual_entry_created.

2026-06-20 — PR 2 sweep step 4: converted _undo_manual_entry_created.

  Handler: app/backend/services/undo_service.py — _undo_manual_entry_created
    - Signature changed from (workspace_path: Path, event: dict) -> dict[str, str]
      to (config: AppConfig, event: dict) -> str.
    - Body wrapped in `with journal_writer.mutate(paths=[journal_path], tag="undo",
      event_type=f"{forward_type}.compensated.v1") as mut:`. Read, header build
      (date + payee → "YYYY-MM-DD payee"), header locate, block locate, blank-line
      consume, block removal, and journal write all happen inside the block.
      UndoFailedError raised inside the block (e.g. header not located) triggers
      writer rollback.
    - Pre-block validation (missing date/payee, missing or empty journal_refs)
      stays outside `mutate` so an incomplete-payload rejection neither backs up
      nor emits.
    - Legacy `backup_file(journal_path, "undo")` call dropped — writer owns
      backup + rollback now.
    - Sets `mut.summary` (f"Undid: {forward_summary}"), `mut.payload`
      (forward payload + compensated_event_id), `mut.compensates =
      forward_event_id` inside the block. Writer emits the compensating event
      with assembled journal_refs on successful exit.

  Dispatcher: _WRITER_HANDLERS now contains transaction.deleted.v1,
  transaction.recategorized.v1, transaction.account_reassigned.v1,
  transaction.status_toggled.v1, and manual_entry.created.v1. No further
  dispatcher changes — set-lookup discrimination handles the new entry
  transparently.

  Tests: test_undo_service.TestUndoManualEntryCreated.test_round_trip_create_
  and_delete passes unchanged. Full undo + journal_writer focused: 43 tests
  pass. Full backend suite: 804 passed, 0 failures.

  Next: convert _undo_transaction_notes_updated.

2026-06-20 — PR 2 sweep step 5: converted _undo_transaction_notes_updated.

  Handler: app/backend/services/undo_service.py — _undo_transaction_notes_updated
    - Signature changed from (workspace_path: Path, event: dict) -> dict[str, str]
      to (config: AppConfig, event: dict) -> str.
    - Body wrapped in `with journal_writer.mutate(paths=[journal_path], tag="undo",
      event_type=f"{forward_type}.compensated.v1") as mut:`. Read, header locate,
      block locate, current-notes-line search, set/insert/remove of the `; notes:`
      line, and journal write all happen inside the block. UndoFailedError raised
      inside the block (header not located / ambiguous) triggers writer rollback.
    - Pre-block validation stays outside `mutate`:
        * pre-migration event missing `previous_notes` key — fails closed before
          backup or emission.
        * empty `journal_path` or `header_line` — rejected before backup.
      Payload-shape rejection neither backs up nor emits a compensating event.
    - Legacy `backup_file(journal_path, "undo")` call dropped — writer owns
      backup + rollback now.
    - Sets `mut.summary` (f"Undid: {forward_summary}"), `mut.payload`
      (forward payload + compensated_event_id), `mut.compensates =
      forward_event_id` inside the block. Writer emits the compensating event
      with assembled journal_refs on successful exit.

  Dispatcher: _WRITER_HANDLERS now contains transaction.deleted.v1,
  transaction.recategorized.v1, transaction.account_reassigned.v1,
  transaction.status_toggled.v1, manual_entry.created.v1, and
  transaction.notes_updated.v1. No further dispatcher changes — set-lookup
  discrimination handles the new entry transparently.

  Tests: test_undo_service.TestUndoNotesUpdated suite (4 tests: round-trip set
  + remove, round-trip replace, round-trip restore after deletion, pre-migration
  fail-closed) passes unchanged. Full undo + journal_writer focused: 43 tests
  pass. Full backend suite: 804 passed, 0 failures.

  Next: convert _undo_transaction_unmatched (multi-file: main journal +
  archive — only multi-path undo handler remaining).

2026-06-20 — PR 2 sweep step 6: converted _undo_transaction_unmatched (multi-file).

  Handler: app/backend/services/undo_service.py — _undo_transaction_unmatched
    - Signature changed from (workspace_path: Path, event: dict) -> dict[str, str]
      to (config: AppConfig, event: dict) -> str. First multi-path undo handler
      converted — pressure-tests the writer's `paths=[journal, archive]` contract
      under conditional pre-existence (archive is absent in the post-unmatch
      state because the original unmatch removed its only entry).
    - Body wrapped in `with journal_writer.mutate(paths=[journal_path,
      archive_path], tag="undo", event_type=f"{forward_type}.compensated.v1") as
      mut:`. Inside the block: re-stamp the imported transaction in the main
      journal (insert :manual: + match-id: tags after the header, rewrite
      destination posting from Expenses:Unknown to the original category),
      locate + remove the restored manual entry (with preceding blank-line
      consume), write the journal, then call archive_manual_entry to re-create
      the archive entry. UndoFailedError raised inside the block (header not
      located on either pass) triggers writer rollback of both files — the
      main journal restores from its backup, the archive is deleted if the
      block had created it.
    - Pre-block validation stays outside `mutate`:
        * missing journal_path / header_line / match_id / restored_manual_block
          — incomplete payload, rejected before backup or emission.
        * no category recoverable from restored_manual_block (heuristic:
          Expenses:/Income:/Revenue:) — rejected before backup or emission.
    - Legacy `backup_file(journal_path, "undo")` and
      `backup_file(archive_path, "undo")` calls dropped — writer owns backup +
      rollback for both files now.
    - Sets `mut.summary` (f"Undid: {forward_summary}"), `mut.payload`
      (forward payload + compensated_event_id), `mut.compensates =
      forward_event_id` inside the block. Writer emits the compensating event
      with assembled journal_refs (both paths included when changed) on
      successful exit.

  Behavioral note: the legacy dispatcher filtered the compensating event's
  journal_refs to entries also present in forward.journal_refs (which for
  unmatch held only the main journal). The writer instead iterates the declared
  `paths` and includes every path whose hash changed — so the compensating
  event now carries both the journal_path and archive_path refs (the latter
  with hash_before "sha256:none" since the archive was absent pre-mutation).
  This is more accurate; the test_compensating_event_emitted test passes
  unchanged because it only asserts existence + type.

  Dispatcher: _WRITER_HANDLERS now contains transaction.deleted.v1,
  transaction.recategorized.v1, transaction.account_reassigned.v1,
  transaction.status_toggled.v1, manual_entry.created.v1,
  transaction.notes_updated.v1, and transaction.unmatched.v1. Every undo
  handler now flows through the writer protocol — the legacy emit-on-success
  branch in undo_event is dead code once PR 2 sweeps the route surface, but
  it will be retained until the last forward route conversion lands (so the
  diff that drops the branch is contiguous with the conversions).

  Imports: dropped unused `backup_file` (from services.backup_service) and
  `rel_path` (from services.event_log_service) imports from undo_service.py
  — neither has any remaining caller in this module.

  Tests: TestUndoUnmatched (2 tests: round-trip unmatch+rematch, compensating
  event emitted) passes unchanged. Full undo + journal_writer focused: 43
  tests pass. Full backend suite: 804 passed, 0 failures.

  Next: sweep the remaining ~14 mutation routes in main.py. After that, drop
  backup_service.py, drop restore_from_backup from event_log_service exports,
  and drop the legacy-handler branch from undo_event.

2026-06-20 — PR 2 sweep step 7: converted transactions_toggle_status route.
  First forward-mutation route conversion (all six undo handlers are now
  writer-routed; the route surface in main.py begins its sweep here).

  Route: app/backend/main.py — transactions_toggle_status
    - Wrapped read + header locate + write in `with journal_writer.mutate(
      paths=[journal_path], tag="toggle-status",
      event_type="transaction.status_toggled.v1") as mut:`.
    - Pre-block validation stays outside `mutate`:
        * journal file existence (404 if not a file).
        * `parse_header(req.headerLine)` (400 if invalid).
        * `set_header_status` round-trip through `_HEADER_RE` (500 if the
          rewritten header is malformed).
      Each rejection is shape/programming-error — neither backs up nor emits.
    - Inside the block: read journal, `_locate_header`, splice the new header
      line in, write the journal. `_locate_header` raises HTTPException(404)
      on miss — that escapes the block and the writer rolls back.
    - Removed `check_drift` and `hash_file` calls (writer owns both).
    - Removed `emit_event` call + its swallowed-exception try/except — the
      writer is now the sole emitter on this path, and emission failure is a
      real error rather than a silent log line. Return value still surfaces
      `eventId` via `mut.event_id`.

  Imports: no new imports needed (`journal_writer` was already imported for
  the reconciliation route conversion). Note that `backup_file`, `check_drift`,
  `hash_file`, `rel_path` remain imported in main.py — they are still used by
  the ~13 unconverted routes (delete/recategorize/reassign-account/notes/unmatch/
  manual-create/import-apply/unknowns-apply/rule-history-apply/etc).

  Tests: full backend suite — 804 passed, 0 failures. No existing test was
  shape-changed (toggle-status has no dedicated route test; the converted
  `_undo_transaction_status_toggled` round-trip test exercises the symmetric
  path and continues to pass).

  Next: convert transactions_delete route (next-simplest single-file mutation,
  same shape — read + header locate + block-find + write + emit).

2026-06-20 — PR 2 sweep step 8: converted transactions_delete route.

  Route: app/backend/main.py — transactions_delete
    - Wrapped read + header locate + block find + splice + write in
      `with journal_writer.mutate(config=config, paths=[journal_path],
      tag="delete", event_type="transaction.deleted.v1") as mut:`.
    - Pre-block validation stays outside `mutate`:
        * journal file existence (404 if not a file).
        * `parse_header(req.headerLine)` — for the summary payee/date only;
          missing header parse falls back to a header-line slice (no rejection).
    - Inside the block: read journal, `_locate_header`, `find_transaction_block`,
      capture deleted_block text, consume a preceding blank line to avoid
      double-blank-line gaps, splice it out, write the journal.
      `_locate_header` raises HTTPException(404) on miss — that escapes the
      block and the writer rolls back.
    - Removed `check_drift`, `backup_file(journal_path, "delete")`, and
      `hash_file` calls — writer owns all three.
    - Removed `emit_event` call + its swallowed-exception try/except — the
      writer is the sole emitter on this path, and emission failure is now a
      real error rather than a silent log line. Returns `mut.event_id`.
    - Payload shape unchanged (journal_path + header_line + deleted_block),
      which keeps `_undo_transaction_deleted` reading the same fields.

  Imports: no new imports needed. `backup_file`, `check_drift`, `hash_file`,
  `rel_path` remain imported in main.py — still used by the ~12 unconverted
  routes.

  Tests: full backend suite — 804 passed, 0 failures. The
  TestReconcileDeleteRoundTrip.test_reconcile_then_delete_yields_byte_equivalent
  _journal test (which exercises this route end-to-end after a reconcile)
  passes unchanged.

  Next: convert transactions_recategorize route (similar shape — read + header
  locate + posting-rewrite + write + emit). Pre-block validation will include
  the "uncategorized" / "transfer" / "split-transaction" 422 checks; the
  posting-rewrite and journal write happen inside `mutate`.

2026-06-20 — PR 2 sweep step 9: converted transactions_recategorize route.

  Route: app/backend/main.py — transactions_recategorize
    - Wrapped read + header locate + block find + posting walk + rewrite +
      write in `with journal_writer.mutate(config=config, paths=[journal_path],
      tag="recategorize", event_type="transaction.recategorized.v1") as mut:`.
    - Pre-block validation stays outside `mutate`:
        * journal file existence (404 if not a file).
        * `parse_header(req.headerLine)` for summary payee/date_str (no rejection;
          falls back to a 60-char slice on parse miss).
        * tracked-account ledger-name collection from `config.tracked_accounts`
          (config-only; used inside the block to skip source postings).
    - File-content-dependent rejections stay **inside** the block — design
      decision: these checks require reading the journal and walking postings,
      so they can't be moved outside. They raise `HTTPException`, escape the
      block, and the writer rolls back from backup. The restore is a no-op
      against disk state (every 422 fires before the `journal_path.write_text`
      call), but backup is captured pre-mutation so rollback is correct in
      principle. No compensating event is emitted on rejection.
      In-block rejections:
        * 422 "Transaction is already uncategorized" — destination posting is
          already Expenses:Unknown and no new category was provided.
        * 422 "Cannot re-categorize a split transaction" — multiple non-source
          postings detected during the block walk.
        * 422 "Cannot re-categorize a transfer" — no destination posting found
          (all non-tracked postings absent).
        * 422 "Transaction already has this category" — previous_account equals
          target_account.
        * 500 "Failed to rewrite posting account" — `rewrite_posting_account`
          returned `changed=False`.
        * 404 from `_locate_header` (header not found in file).
    - Inside the block: read, header locate, block parse, posting walk (with
      the four 422 checks), `rewrite_posting_account` (500 on failure), write.
    - Removed `check_drift`, `backup_file(journal_path, "recategorize")`, and
      `hash_file` calls — writer owns all three.
    - Removed `emit_event` call + its swallowed-exception try/except — the
      writer is the sole emitter on this path, and emission failure is now a
      real error rather than a silent log line. Returns `mut.event_id`.
    - Payload shape unchanged (journal_path + header_line + previous_account +
      new_account), which keeps `_undo_transaction_recategorized` reading the
      same fields.
    - Captured local variables `previous_account` and `target_account` are
      hoisted before the `with` block so the `return` statement after the block
      can reference them; they're populated inside the block before the journal
      write.

  Imports: no new imports needed. `backup_file`, `check_drift`, `hash_file`,
  `rel_path` remain imported in main.py — still used by the ~11 unconverted
  routes (reassign-account/notes/unmatch/manual-create/import-apply/
  unknowns-apply/rule-history-apply/etc).

  Tests: tests/test_notes_and_recategorize.py and tests/test_transaction_
  actions.py both pass unchanged (the notes_and_recategorize file's
  recategorize-related tests exercise the underlying parser components, not
  the route directly, so they're orthogonal). The
  `_undo_transaction_recategorized` round-trip test exercises the
  recategorize→undo path end-to-end and continues to pass. Full backend suite:
  804 passed, 0 failures.

  Next: convert transactions_reassign_account route (same posting-rewrite
  shape as recategorize but rewrites the source/tracked posting; pre-block
  validation includes the "not a tracked account" 422 check).

2026-06-20 — PR 2 sweep step 10: converted transactions_reassign_account route.

  Route: app/backend/main.py — transactions_reassign_account
    - Wrapped read + header locate + block find + source-posting walk + rewrite +
      write in `with journal_writer.mutate(config=config, paths=[journal_path],
      tag="reassign-account", event_type="transaction.account_reassigned.v1")
      as mut:`.
    - Pre-block validation stays outside `mutate`:
        * journal file existence (404 if not a file).
        * `parse_header(req.headerLine)` for summary payee/date_str (no rejection;
          falls back to a 60-char slice on parse miss).
        * tracked-account ledger-name collection from `config.tracked_accounts`
          (config-only).
        * 422 "Not a tracked account" — destination ledger name not in
          `tracked_ledger_accounts`. Config-only payload-shape check; neither
          backs up nor emits.
    - File-content-dependent rejections stay **inside** the block — same design
      decision as the recategorize conversion: these checks require reading the
      journal and walking postings, so they can't be moved outside. They raise
      `HTTPException`, escape the block, and the writer rolls back from backup.
      The restore is a no-op against disk state (every 422 fires before the
      `journal_path.write_text` call), but backup is captured pre-mutation so
      rollback is correct in principle. No compensating event is emitted on
      rejection. In-block rejections:
        * 422 "Cannot reassign a multi-account transaction" — multiple source
          (tracked) postings detected during the block walk.
        * 422 "Cannot reassign account on this transaction" — no source posting
          found (e.g. uncategorized-only blocks; should not occur for real txns).
        * 422 "Transaction already belongs to this account" — previous_account
          equals `req.newAccountLedgerName`.
        * 500 "Failed to rewrite posting account" — `rewrite_posting_account`
          returned `changed=False`.
        * 404 from `_locate_header` (header not found in file).
    - Inside the block: read, header locate, block parse, source-posting walk
      (with the multi-account 422 and previous_account capture), the no-source
      422, the already-belongs 422, `rewrite_posting_account` (500 on failure),
      write.
    - Removed `check_drift`, `backup_file(journal_path, "reassign-account")`,
      and `hash_file` calls — writer owns all three.
    - Removed `emit_event` call + its swallowed-exception try/except — the
      writer is the sole emitter on this path, and emission failure is now a
      real error rather than a silent log line. Returns `mut.event_id`.
    - Payload shape unchanged (journal_path + header_line + previous_account +
      new_account), which keeps `_undo_transaction_account_reassigned` reading
      the same fields.
    - Captured local variable `previous_account` is hoisted before the `with`
      block so the `return` statement after the block can reference it; it's
      populated inside the block before the journal write.

  Imports: no new imports needed. `backup_file`, `check_drift`, `hash_file`,
  `rel_path` remain imported in main.py — still used by the ~10 unconverted
  routes (notes/unmatch/manual-create/import-apply/unknowns-apply/
  rule-history-apply/etc).

  Tests: full backend suite — 804 passed, 0 failures. No dedicated route test
  for reassign-account; the converted `_undo_transaction_account_reassigned`
  handler exercises the symmetric undo path. No existing test was shape-changed.

  Next: convert transactions_notes route (notes update — same single-file
  header-locate + line-edit shape as toggle-status, with the `; notes:`
  comment-line set/insert/remove logic mirroring the undo handler).

2026-06-20 — PR 2 sweep step 11: converted transactions_notes route.

  Route: app/backend/main.py — transactions_notes
    - Wrapped read + header locate + block find + notes-line search + set/
      insert/remove of the `; notes:` comment line + write in
      `with journal_writer.mutate(config=config, paths=[journal_path],
      tag="notes", event_type="transaction.notes_updated.v1") as mut:`.
    - Pre-block validation stays outside `mutate`:
        * journal file existence (404 if not a file).
        * `parse_header(req.headerLine)` for summary payee/date_str (no
          rejection; falls back to a 60-char slice on parse miss).
    - Inside the block: read journal, `_locate_header`, `find_transaction_block`,
      walk the block for an existing `; notes:` line (capturing
      `previous_notes` if present), then:
        * non-empty `req.notes` → overwrite if present or insert after the
          header line;
        * empty `req.notes` → delete the existing notes line if present;
        * no-op if empty and no existing line.
      Write the journal. `_locate_header` raises HTTPException(404) on miss —
      that escapes the block and the writer rolls back.
    - Removed `check_drift`, `backup_file(journal_path, "notes")`, and
      `hash_file` calls — writer owns all three.
    - Removed `emit_event` call + its swallowed-exception try/except — the
      writer is the sole emitter on this path, and emission failure is now a
      real error rather than a silent log line. Returns `mut.event_id`.
    - Payload shape unchanged (journal_path + header_line + notes +
      previous_notes), which keeps `_undo_transaction_notes_updated` reading
      the same fields.

  Imports: no new imports needed. `backup_file`, `check_drift`, `hash_file`,
  `rel_path` remain imported in main.py — still used by the ~9 unconverted
  routes (unmatch/manual-create/import-apply/unknowns-apply/rule-history-apply/
  manual-transfer-resolution/etc).

  Tests: focused undo + journal_writer suite (43 tests) passes; full backend
  suite — 804 passed, 0 failures. The TestUndoNotesUpdated round-trip tests
  exercise the notes→undo symmetric path end-to-end and continue to pass; no
  test was shape-changed.

  Next: convert transactions_unmatch route (multi-file mutation — main journal
  + archived-manual.journal; pressure-tests forward-route use of the writer's
  `paths=[journal, archive]` contract that was first exercised by the
  _undo_transaction_unmatched conversion).

2026-06-20 — PR 2 sweep step 12: converted transactions_unmatch route (first
  multi-file forward-route conversion).

  Route: app/backend/main.py — transactions_unmatch
    - Wrapped the entire unmatch sequence (archive locate + main-journal
      tag-strip + posting rewrite + first write + restored-entry insertion +
      second write + archive-block removal or unlink) in
      `with journal_writer.mutate(config=config, paths=[journal_path,
      archive_path], tag="unmatch", event_type="transaction.unmatched.v1") as
      mut:`. Pressure-tests forward-route use of the writer's
      `paths=[journal, archive]` contract — the writer captures hash_before +
      backs up both files at entry, and on rollback restores both byte-for-byte
      (or deletes any newly created file). Symmetric with the
      _undo_transaction_unmatched conversion that introduced this multi-file
      pattern on the undo side.
    - Pre-block validation stays outside `mutate`:
        * journal file existence (404 if not a file).
        * archive file existence (404 if not a file) — config-only check
          ("No archived entries exist") fires before backup or emission.
        * tracked_ledger_accounts collection from config.tracked_accounts
          (config-only; used inside the block to identify destination posting).
        * `parse_header(req.headerLine)` for summary payee/date_str (no
          rejection; falls back to a 60-char slice on parse miss).
    - File-content-dependent rejections stay **inside** the block — they raise
      `HTTPException`, escape the block, and the writer rolls back both files
      from backup. The restores are no-ops against disk state (both 404s fire
      before either file's `write_text` call), but backups are captured
      pre-mutation so rollback is correct in principle. No event is emitted
      on rejection. In-block rejections:
        * 404 "Archived manual entry not found for this match" — match-id tag
          not found anywhere in archive_lines (e.g. stale match-id).
        * 404 from `_locate_header` (matched-transaction header not found in
          the main journal).
    - Inside the block: read archive_text, scan for the match-id tag + extract
      archived_block_lines; read main journal, locate header, find block,
      walk postings to collect tag indices and destination_idx; remove tag
      lines, rewrite destination posting to Expenses:Unknown; write main
      journal; re-read main journal, find date-ordered insertion point, insert
      the restored manual entry (stripped of the match-id tag); write main
      journal again; remove the archived block from archive_lines (with
      trailing-blank-line consume), write archive (or unlink if no non-empty
      non-comment lines remain).
    - Removed `check_drift(workspace, journal_path)` and
      `check_drift(workspace, archive_path)` calls — writer owns drift for
      both paths.
    - Removed `backup_file(journal_path, "unmatch")` and
      `backup_file(archive_path, "unmatch")` calls — writer owns backup for
      both paths.
    - Removed `hash_file(journal_path)` and `hash_file(archive_path)` calls
      and the manual `refs` list assembly — writer assembles journal_refs
      directly from path-set hash deltas.
    - Removed `emit_event` call + its swallowed-exception try/except — the
      writer is the sole emitter on this path, and emission failure is now a
      real error rather than a silent log line. Returns `mut.event_id`.
    - Payload shape unchanged (journal_path + archive_path + header_line +
      match_id + restored_manual_block), which keeps
      `_undo_transaction_unmatched` reading the same fields.
    - Redundant inline `from services.transfer_service import ACCOUNT_LINE_RE,
      ACCOUNT_ONLY_RE` import dropped — these are already imported at the
      module top (line 58, alongside `rewrite_posting_account`).

  Behavioral note (archive-deleted journal_refs): The legacy route
  conditionally appended the archive ref to the emitted event only when
  `archive_path.is_file()` post-mutation — so when the unmatched entry was
  the last archive entry (archive deleted), the event carried only the main
  journal ref. The writer's rule is path-set agnostic: include any declared
  path whose hash_after differs from hash_before. When the archive is
  deleted, hash_after becomes "sha256:none" (hash_file gracefully handles
  missing files), which differs from the captured hash_before, so the
  archive ref is included with hash_after = "sha256:none". This is more
  accurate (the archive *was* mutated, even if to deletion). Symmetric with
  the _undo_transaction_unmatched conversion's documented behavioral note:
  the same rule applies on the inverse direction when the archive is
  re-created.

  Imports: no new imports needed. `backup_file`, `check_drift`, `hash_file`,
  `rel_path` remain imported in main.py — still used by the ~8 unconverted
  routes (manual-create / import-apply / unknowns-apply / rule-history-apply /
  manual-transfer-resolution / etc).

  Tests: focused suite (test_transaction_actions + test_undo_service +
  test_journal_writer): 65 passed, 0 failures. Full backend suite: 804
  passed, 0 failures. The test_transaction_actions tests for unmatch
  exercise the underlying journal-mutation logic directly (not the HTTP
  route), so they were not shape-changed by the conversion.

  Next: continue the route sweep — the remaining ~8 mutation routes
  (manual-create, import-apply, unknowns-apply, rule-history-apply,
  manual-transfer-resolution, etc). After the route sweep is complete,
  drop backup_service.py, drop restore_from_backup from event_log_service
  exports, and drop the legacy-handler branch from undo_event.

2026-06-20 — PR 2 sweep step 13: converted transactions_create route (manual
  entry creation; first route conversion where the target journal file may
  not exist pre-mutation).

  Route: app/backend/main.py — transactions_create
    - Wrapped the call to `create_manual_transaction(...)` in
      `with journal_writer.mutate(config=config, paths=[journal_path],
      tag="manual-entry", event_type="manual_entry.created.v1") as mut:`.
    - First conversion where the target journal file may not exist
      pre-mutation: when the user creates the first transaction of a new
      year, `<year>.journal` is born by this route. The writer's existing
      `pre_existed` / `hash_before == "sha256:none"` bookkeeping handles
      that case, and rollback policy for not-pre-existed paths is to delete
      the file if the block created it — matching the legacy "no journal
      should exist if create failed" semantics.
    - Pre-block validation stays outside `mutate`:
        * tracked-account existence (404 if `config.tracked_accounts.get(
          req.trackedAccountId)` is None) — config-only payload-shape check;
          neither backs up nor emits.
        * year extraction + journal_path / accounts_dat / currency /
          source_account derivation — pure value extraction, no I/O.
    - `ValueError` from `create_manual_transaction` (invalid amount string
      via `_parse_amount_str`, missing tracked ledger account) stays **inside**
      the block — caught and re-raised as `HTTPException(400)`, escapes the
      block, and the writer rolls back (no-op against disk because both
      ValueError paths fire before the in-service `journal_path.write_text`).
      Backup is captured pre-mutation so rollback is correct in principle.
      No event is emitted on rejection.
    - Inside the block: `create_manual_transaction(...)` (which handles its
      own parent.mkdir, optional `""` write to ensure file exists, read +
      block insertion + write), then sets `mut.summary` and `mut.payload`.
    - Removed `check_drift(config.root_dir, journal_path)` call — writer
      owns drift detection.
    - Removed `hash_file(journal_path)` call and the manual `journal_refs`
      assembly — writer assembles journal_refs from path-set hash deltas.
    - Removed `emit_event` call + its swallowed-exception try/except — the
      writer is the sole emitter on this path, and emission failure is now
      a real error rather than a silent log line. Returns `mut.event_id`.
    - Payload shape unchanged (date / payee / amount / currency /
      destination_account / source_account), which keeps
      `_undo_manual_entry_created` reading the same fields.
    - `result: dict` declared before the `with` block so the `return`
      statement after the block can reference it; populated inside the
      block before the journal write returns.

  Imports: no new imports needed (`journal_writer` was already imported).
  `backup_file`, `check_drift`, `hash_file`, `rel_path` remain imported in
  main.py — still used by the ~7 unconverted routes (manual-transfer-
  resolution / import-apply / import-undo / unknowns-apply /
  rule-history-apply).

  Tests: focused suite (test_manual_entry_service + test_undo_service +
  test_journal_writer): 65 passed, 0 failures. The
  TestUndoManualEntryCreated round-trip test exercises the create→undo
  symmetric path end-to-end at the service layer and continues to pass.
  Full backend suite: 804 passed, 0 failures. No existing test was
  shape-changed.

  Next: continue the route sweep — the remaining ~7 mutation routes
  (manual-transfer-resolution is being removed in PR 3 so it can be
  skipped; import-apply, import-undo, unknowns-apply, rule-history-apply
  remain). After the route sweep is complete, drop backup_service.py,
  drop restore_from_backup from event_log_service exports, and drop the
  legacy-handler branch from undo_event.

2026-06-20 — PR 2 sweep step 14: converted import_apply route (CSV-derived bulk
  append; first conversion of a route whose mutation drives the import history
  index and stage store as side effects).

  Route: app/backend/main.py — import_apply
    - Wrapped apply_import(config, stage) + archive_inbox_csv + stage status/
      result mutation + record_applied_import + stages.save in
      `with journal_writer.mutate(config=config, paths=[journal], tag="import",
      event_type="import.applied.v1") as mut:`.
    - Pre-block validation stays outside `mutate`:
        * stage lookup (404 on FileNotFoundError from stages.load).
        * stage kind mismatch (400 if stage["kind"] \!= "import").
        * idempotent early return when stage["status"] == "applied" — the route
          must not double-back-up nor double-emit when called a second time.
        * Path extraction for `journal` (pure value derivation, no I/O).
    - Like transactions_create, the target journal may not exist pre-mutation
      (bulk-append into a new <year>.journal). The writer's
      `pre_existed`/`hash_before == "sha256:none"` bookkeeping handles that
      case, and rollback policy for not-pre-existed paths is to delete the
      file if the block created it.
    - In-block error handling:
        * `CommandError` from `apply_import` — caught and re-raised as
          `HTTPException(400)`. Escapes the block; writer rolls back (deletes
          journal if newly created, restores from backup otherwise). No event
          is emitted on rejection.
        * `OSError` from `archive_inbox_csv` — caught locally and captured to
          `source_csv_warning`. Does NOT escape the block. Per PRD §6 "Scope —
          journal-class only" and the explicit "Out of Scope" note on
          archive CSV moves, the archive operation is intentionally outside
          the writer's transactional boundary: a failure to move the inbox
          CSV must not roll back the journal write (the import is successful;
          the user is simply warned that the source CSV stayed in inbox).
    - `record_applied_import` + `stages.save` stay **inside** the block:
        * Ordering: `record_applied_import` must happen before `mut.payload`
          assembly because the payload references `history_entry["id"]`.
        * Failure semantics: if either raises, the journal rolls back —
          we don't want a journal-with-imports + stage-says-staged state.
          (The history JSON / state.db could be in a partial state on
          failure, but that is no worse than the legacy code which had no
          rollback at all for these side effects.)
    - `mut.payload` mirrors the legacy `emit_event` payload (journal_path,
      source_file, account_id, transactions_added, duplicates_skipped,
      conflicts, history_id). The writer assembles `journal_refs` from
      path-set hash deltas — the manual `journal_refs=[{path, hash_before,
      hash_after}]` argument is gone, and the local `hash_before` / `hash_after`
      captures are no longer needed.
    - `check_drift(config.root_dir, journal)`, `backup_file(journal, "import")`,
      `hash_file(journal)` calls all removed — writer owns drift, hashing,
      and backup.
    - `emit_event` call + its swallowed-exception try/except dropped — the
      writer is the sole emitter on this path, and emission failure is now a
      real error rather than a silent log line.
    - `stage["result"]["backupPath"]` set unconditionally to `None`. The
      writer manages backups internally and intentionally does not surface
      their paths — backups are an implementation detail of the rollback
      mechanism, not a user-facing artifact. Verified consumers:
        * `record_applied_import` reads `result.get("backupPath")` — None
          passes through to the history entry without raising.
        * Frontend ImportFlow.svelte renders `{#if entry.backupPath}` — null
          is handled gracefully (the "Backup: …" line just no longer renders).
        * No backend code reads the backupPath as a restore target (undo_import
          builds its own undo backup via backup_file internally; the import
          history entry's backupPath has been a forensic/display field only).

  Imports: no new imports needed. `backup_file`, `check_drift`, `hash_file`,
  `rel_path` remain imported in main.py — still used by the ~6 unconverted
  routes (import-undo / unknowns-apply / rule-history-apply / manual-transfer-
  resolution-apply / etc).

  Tests: focused suite (test_import_archive + test_import_identity +
  test_import_history_service + test_journal_writer): 55 passed, 0 failures.
  The test_import_archive suite exercises `apply_import` directly at the
  service layer (not via the HTTP route), so the route-level conversion
  doesn't touch those assertions. Full backend suite: 804 passed, 0 failures.
  No existing test was shape-changed.

  Next: continue the route sweep — remaining ~6 mutation routes
  (import_undo, unknown_apply, rules_history_apply, and a few others;
  manual-transfer-resolution is being removed in PR 3 so it can be skipped).
  After the route sweep is complete, drop backup_service.py, drop
  restore_from_backup from event_log_service exports, and drop the
  legacy-handler branch from undo_event.

2026-06-20 — PR 2 sweep step 15: converted import_undo route.

  Route: app/backend/main.py — import_undo
    - Wrapped the `undo_import(config, req.historyId)` call in
      `with journal_writer.mutate(config=config, paths=[journal_path],
      tag="import-undo", event_type="import.undone.v1") as mut:`.
    - Pre-block validation stays outside `mutate`:
        * resolve `journal_path` from `list_import_history(config)` by
          `req.historyId` — single scan extracts the entry's
          `targetJournalPath`. If the entry isn't found OR the entry has no
          `targetJournalPath`, raise HTTPException(404) before entering the
          block. This collapses the legacy two-step "history not found"
          KeyError + "missing target" cases into a single 404 status, matching
          legacy behavior (the legacy code would also return 404 in both
          cases — once via `undo_import`'s KeyError surface).
        * Legacy best-effort pre-resolution `try/except Exception` swallow
          (which would log-and-continue, silently skipping emission if
          `list_import_history` raised) is dropped — resolution errors now
          surface as 500s rather than producing a silent successful undo with
          no event.
    - In-block error handling:
        * `KeyError` from `undo_import` (defensive — pre-block validation
          ensures the entry exists, but `undo_import` re-validates and could
          theoretically raise on race) — caught and re-raised as
          HTTPException(404). Writer rolls back from backup (no-op since
          mutation hasn't happened yet).
        * `FileNotFoundError`/`OSError`/`ValueError` from `undo_import` —
          caught and re-raised as HTTPException(400). Writer rolls back the
          journal from backup (restores byte-for-byte if `undo_import` wrote
          partial state before raising, no-op otherwise).
    - Inside the block: call `undo_import(config, req.historyId)` (which
      handles its own internal backup_file call, journal read+strip+write,
      import index update, archive CSV restore, and history entry update),
      then read `entry["undo"]["removedTxnCount"]` for the payload and set
      `mut.summary` + `mut.payload`.
    - Removed `check_drift(config.root_dir, journal_path)` call — writer
      owns drift detection.
    - Removed `hash_file(journal_path)` call and manual `journal_refs`
      assembly — writer assembles `journal_refs` from path-set hash deltas.
    - Removed `emit_event` call + its swallowed-exception try/except — the
      writer is the sole emitter on this path, and emission failure is now
      a real error rather than a silent log line.
    - Returns `{"entry": entry}` unchanged. Note that this route did not
      surface an `eventId` in its response (legacy behavior); the writer-
      minted `mut.event_id` is recoverable via the events log if needed.
    - Payload shape unchanged (journal_path + history_id +
      transactions_removed). Summary copy unchanged (matches legacy
      "Undid import {id}: removed {N} transactions").
    - `undo_import`'s internal `backup_file(journal_path, "undo")` is left
      untouched — harmless duplicate backup until PR 2 sweeps
      backup_service.py. Its `undoBackupPath` is still asserted by
      `test_import_history_service.test_undo_import_round_trip` and remains
      user-facing forensic data on the history entry (frontend ImportFlow.
      svelte renders it conditionally).

  Imports: no new imports needed (`journal_writer` was already imported).
  `backup_file`, `check_drift`, `hash_file`, `rel_path` remain imported in
  main.py — still used by the ~5 unconverted routes (unknown_apply / rules_
  history_apply / manual-transfer-resolution-apply / etc).

  Tests: focused suite (test_import_history_service + test_journal_writer +
  test_import_archive + test_import_identity): 55 passed, 0 failures. Full
  backend suite: 804 passed, 0 failures. No existing test was shape-changed
  (no HTTP-route-level test for import_undo; the service-layer
  `test_undo_import_round_trip` test continues to exercise `undo_import`
  directly).

  Next: continue the route sweep — remaining ~5 mutation routes
  (unknown_apply [multi-file: journal + accounts_dat + archive],
  rules_history_apply, and the manual-transfer-resolution route which is
  being removed in PR 3 so can be skipped). After the route sweep is
  complete, drop backup_service.py, drop restore_from_backup from
  event_log_service exports, and drop the legacy-handler branch from
  undo_event.

2026-06-20 — PR 2 sweep step 16: converted unknown_apply route (first three-path
  mutation; pressure-tests the writer's co-candidate scope from PRD §6).

  Route: app/backend/main.py — unknown_apply
    - Wrapped apply_unknown_mappings(...) + stage status/result mutation +
      stages.save in `with journal_writer.mutate(config=config,
      paths=[journal_path, accounts_dat, archived_manual], tag="unknowns",
      event_type="unknowns.applied.v1") as mut:`. First three-path mutation —
      pressure-tests PRD §6 ("Scope — journal-class only", co-candidates
      `10-accounts.dat` and `archived-manual.journal` may ride along when an
      op touches them alongside a year journal). The path-set check passes
      because both journal_path (year journal) and archived_manual end in
      `.journal`; accounts_dat is the co-candidate.
    - Pre-block validation stays outside `mutate`:
        * stage lookup (404 on FileNotFoundError from stages.load).
        * stage kind mismatch (400 if stage["kind"] != "unknowns").
        * idempotent early return when stage["status"] == "applied" — the route
          must not double-back-up nor double-emit when called a second time.
        * selections-empty (400 if no mappings staged).
        * path derivation for journal_path / accounts_dat / archived_manual
          (pure value extraction, no I/O).
    - Pre-existence handling for the three paths is heterogeneous and the
      writer's existing `pre_existed` / `hash_before == "sha256:none"`
      bookkeeping covers all three cases uniformly:
        * journal_path: must exist (the stage was built by scanning it).
        * accounts_dat: must exist (rules engine couldn't have run otherwise).
        * archived_manual: may or may not exist (it's lazily created the
          first time a manual entry is archived during an unknowns match).
          Rollback policy for not-pre-existed paths is to delete the file if
          the block created it.
    - In-block error handling:
        * `ValueError` from `apply_unknown_mappings` (e.g. an unknown account
          in `selections`) — caught and re-raised as `HTTPException(400)`.
          Escapes the block; writer rolls back all three paths (journal +
          accounts_dat byte-restore from their backups; archive deleted if
          newly created, byte-restore if pre-existed). No event is emitted
          on rejection.
        * Any unexpected exception from `stages.save` — escapes the block
          unwrapped; writer rolls back all three paths. The stage state in
          `.workflow/state.db` may be in a partial state on failure (per
          PRD §6, `.workflow/` mutations are out of the writer's scope), but
          that is no worse than the legacy code which had no rollback at all
          for stage state. Same trade-off documented for the import_apply
          conversion.
    - `stages.save` stays **inside** the block so a stages.save failure rolls
      back the journal alongside — we don't want a journal-with-mappings-applied
      state with a stage that says staged. Consistent with the ordering
      decision documented for the import_apply conversion (`record_applied_
      import` + `stages.save` inside the block).
    - `mut.summary` and `mut.payload` mirror the legacy `emit_event` arguments
      (mappings_applied, matches_applied, match_ids, warnings, journal_path).
      Both are set inside the block after `apply_unknown_mappings` returns.
    - Removed `check_drift(config.root_dir, journal_path)`,
      `hash_file(accounts_dat)`, `hash_file(archived_manual)` (all three
      pre-mutation hash captures) — writer captures hash_before for every
      declared path on entry.
    - Removed `backup_file(journal_path, "unknowns")` and
      `backup_file(accounts_dat, "rules")` calls — writer owns backup for
      every declared path. Note that the legacy code did **not** back up
      archived_manual (it relied on `apply_unknown_mappings`'s inner
      `rollback_archive` via archive_size_before for archive recovery). The
      writer now backs up archived_manual when it pre-exists, providing a
      uniform rollback story across all three paths. The inner
      `rollback_archive` inside `apply_unknown_mappings` is left untouched —
      harmless (in-place truncate is dominated by the writer's byte-restore
      from its pre-mutation backup), and removed when PR 2 sweeps
      `backup_service.py`.
    - Removed the post-mutation `hash_file` calls (all three) and the manual
      `refs` list assembly with the conditional archive append — writer
      assembles `journal_refs` from path-set hash deltas directly.
    - Removed `emit_event` call + its swallowed-exception try/except — the
      writer is the sole emitter on this path, and emission failure is now
      a real error rather than a silent log line.
    - `stage["result"]["backupPaths"]` is now `None` (was a 2-element list
      of journal_backup + accounts_backup resolve paths). The writer manages
      backups internally and intentionally does not surface their paths.
      Verified no consumer reads this field: zero references in
      app/backend/tests/, zero references in app/frontend/, and the only
      remaining backend reference is the assignment site itself.

  Behavioral note (journal_refs filtering): The legacy route unconditionally
  emitted both the `journal_path` ref and the `accounts_dat` ref (regardless
  of whether accounts_dat actually changed — e.g. when all selections were
  `match` not `category`, accounts_dat is untouched but its ref was still in
  the event), and conditionally appended the `archive` ref only when its
  hash changed. The writer's rule is path-set-agnostic: include any declared
  path whose hash_after differs from hash_before. So the new behavior is:
    * journal_path ref: included (always changes — at minimum a tag stamp).
    * accounts_dat ref: included only when changed (matches with category
      selections that touch payee rules; omitted for match-only applies).
    * archive ref: included when its hash changes (same as legacy), with
      the writer naturally handling the "archive newly created" case via
      hash_before = "sha256:none".
  This is more accurate (the legacy unconditionally-include-accounts_dat
  ref implied a write that didn't happen). No test was shape-changed.

  Imports: no new imports needed (`journal_writer` was already imported).
  `backup_file`, `check_drift`, `hash_file`, `rel_path` remain imported in
  main.py — still used by the ~3 unconverted routes (rules_history_apply
  remains; manual-transfer-resolution-apply is being removed in PR 3).

  Tests: focused suite (test_unknowns_service + test_unknown_stage_resume +
  test_journal_writer + test_undo_service): 66 passed, 0 failures. The
  test_unknowns_service suite exercises `apply_unknown_mappings` directly at
  the service layer (not via the HTTP route), so the route-level conversion
  doesn't touch those assertions. test_unknown_stage_resume exercises the
  stage scan + resume flow at the HTTP level (via FastAPI's TestClient),
  including the early-return path when status == "applied" — passes
  unchanged. Full backend suite: 804 passed, 0 failures.

  Next: continue the route sweep — remaining ~3 mutation routes
  (rules_history_apply [single-file journal write with co-read of accounts_dat];
  manual-transfer-resolution-apply is being removed in PR 3 so skipped;
  there may be one or two more from the long tail). After the route sweep is
  complete, drop backup_service.py, drop restore_from_backup from
  event_log_service exports, and drop the legacy-handler branch from
  undo_event.

2026-06-20 — PR 2 sweep step 17: converted rules_history_apply route (last
  remaining non-skipped mutation route — route surface sweep now complete).

  Route: app/backend/main.py — rules_history_apply
    - Wrapped apply_rule_reapply(...) + stage status/result mutation +
      stages.save in `with journal_writer.mutate(config=config,
      paths=[journal_path], tag="rule-history",
      event_type="rule.history_applied.v1") as mut:`.
    - Pre-block validation stays outside `mutate`:
        * stage lookup (404 on FileNotFoundError from stages.load).
        * stage kind mismatch (400 if stage["kind"] != "rule_history").
        * idempotent early return when stage["status"] == "applied" — the
          route must not double-back-up nor double-emit when called a second
          time.
        * selections-empty (400 if no historical matches selected).
        * path derivation for journal_path (pure value extraction, no I/O).
    - In-block error handling:
        * `ValueError` from `apply_rule_reapply` (no selections, missing
          candidates, unknown account in selection) — caught and re-raised
          as `HTTPException(400)`. Escapes the block; writer rolls back from
          backup (no-op against disk because every `ValueError` in
          `apply_rule_reapply` fires before its `journal_path.write_text`
          call). No event is emitted on rejection.
    - `stages.save` stays **inside** the block so a failure rolls back the
      journal alongside — we don't want a journal-with-rule-applied state
      with a stage that says staged. Consistent with the ordering decision
      documented for the import_apply and unknown_apply conversions.
    - `mut.summary` and `mut.payload` mirror the legacy `emit_event`
      arguments (journal_path, transactions_updated, selected_candidate_count,
      warnings). Set inside the block after `apply_rule_reapply` returns and
      after `stages.save` completes.
    - Removed `check_drift(config.root_dir, journal_path)` call — writer
      owns drift detection.
    - Removed `backup_file(journal_path, "rule-history")` call — writer owns
      backup.
    - Removed `hash_file(journal_path)` call and the manual `journal_refs`
      list assembly — writer assembles `journal_refs` from path-set hash
      deltas directly.
    - Removed `emit_event` call + its swallowed-exception try/except — the
      writer is the sole emitter on this path, and emission failure is now a
      real error rather than a silent log line.
    - `stage["result"]["backupPath"]` is now `None` (was a string holding
      `str(journal_backup.resolve())`). The writer manages backups internally
      and intentionally does not surface their paths. This is the first
      conversion where the surfaced `backupPath` was *consumed by the
      frontend* (unlike import_apply where the frontend already gated on
      `{#if entry.backupPath}` and unlike unknown_apply where the
      `backupPaths` field had no consumer at all).

  Frontend: app/frontend/src/routes/unknowns/+page.svelte
    - Type narrowed: `backupPath: string` → `backupPath: string | null` on
      the RuleHistoryStage.result shape (line 151).
    - Display gated: the "Backup: {pathLabel(...)}" suffix on line 1513 is
      now wrapped in `{#if historyStage.result.backupPath} ... {/if}`. When
      backupPath is null (the new norm), the line reads "Updated
      transactions: N." and the Backup suffix is omitted entirely. Matches
      the ImportFlow.svelte pattern (`{#if entry.backupPath}`). Line 304
      (the redirect-URL builder) already gated via optional-chain so needed
      no change.

  Imports: no new imports needed (`journal_writer` was already imported).
  `backup_file`, `check_drift`, `hash_file`, `rel_path` remain imported in
  main.py — `backup_file`/`check_drift`/`hash_file` are still used by the
  unconverted manual-transfer-resolution routes (being removed in PR 3);
  `rel_path` is still used inside the converted routes for payload path
  serialization. Final import cleanup happens when PR 2 drops
  `backup_service.py` and PR 3 removes manual-transfer-resolution.

  Tests: focused suite (test_rule_reapply_service + test_journal_writer +
  test_undo_service): 45 passed, 0 failures. Full backend suite: 804
  passed, 0 failures. Frontend type check (svelte-check): 870 files, 0
  errors, 0 warnings.

  Route sweep status: every mutation route in main.py except
  manual-transfer-resolution-apply (skipped per PRD §Out-of-Scope; being
  removed in PR 3) now flows through `journal_writer.mutate(...)`. The
  legacy `emit_event` call site in manual-transfer-resolution-apply is the
  only remaining direct caller of `emit_event` for a forward mutation in
  main.py.

  Next: drop backup_service.py, drop restore_from_backup from
  event_log_service exports, and drop the legacy-handler branch from
  undo_event (every undo handler is now writer-routed via
  _WRITER_HANDLERS). All three cleanups are gated on the manual-transfer-
  resolution-apply route either being converted or removed — leaving the
  legacy `backup_file` import live for one more PR is acceptable.

2026-06-20 — PR 2 sweep step 18: dropped the legacy-handler branch from
  undo_event. First of the three final PR 2 cleanups (the other two — delete
  backup_service.py, drop restore_from_backup from public exports — remain
  gated on PR 3's removal of manual_transfer_resolution_service).

  Dispatcher: app/backend/services/undo_service.py — undo_event
    - Removed the `if event_type in _WRITER_HANDLERS / else` discriminator
      and the entire legacy branch (~20 lines): the legacy `handler(
      workspace_path, forward) -> dict[str, str]` call, the manual
      journal_refs assembly (pairing forward.journal_refs.hash_after with
      handler-returned post-mutation hashes), and the dispatcher's direct
      `emit_event(...)` call for legacy handlers. The dispatcher now always
      calls `handler(config, forward)` and trusts the handler to have emitted
      via journal_writer.
    - Safe because every entry in `_HANDLERS` (transaction.deleted.v1,
      transaction.recategorized.v1, transaction.account_reassigned.v1,
      transaction.status_toggled.v1, manual_entry.created.v1,
      transaction.unmatched.v1, transaction.notes_updated.v1) was also in
      `_WRITER_HANDLERS` — the else branch was provably dead code.

  Type system cleanup:
    - Removed `_WRITER_HANDLERS: frozenset[str]` — no longer needed since
      there's only one handler protocol.
    - Removed `LegacyHandlerFn = Callable[[Path, dict], dict[str, str]]`
      type alias.
    - Collapsed `HandlerFn = LegacyHandlerFn | WriterHandlerFn` union into
      a single `HandlerFn = Callable[[AppConfig, dict], str]`. Removed the
      intermediate `WriterHandlerFn` name (redundant with the single
      protocol).
    - Dropped `from pathlib import Path` — only referenced by
      `LegacyHandlerFn`, now unused.
    - Dropped `emit_event` from the `services.event_log_service` import
      (still imports `hash_file` for drift verification + `read_events` for
      event log loading). The only `emit_event` caller in this module was
      the legacy branch; every handler now emits inside its own
      `journal_writer.mutate(...)` block.

  Handler protocol simplification (docstring): the in-file commentary
  describing the two-protocol world is gone; the surviving comment on
  `HandlerFn` reads "handlers take ``(config, event)`` and own emission via
  ``journal_writer.mutate``; they return the compensating event id."

  Tests: focused suite (test_undo_service + test_journal_writer): 43 passed.
  Full backend suite: 804 passed, 0 failures. No existing test was
  shape-changed — the test_undo_service round-trip tests (TestUndoDeleted,
  TestUndoRecategorized, TestUndoStatusToggled, TestUndoManualEntryCreated,
  TestUndoNotesUpdated, TestUndoUnmatched) and the dispatcher tests
  (TestUndoDispatcher: not_found, unsupported, already_compensated, drift)
  all continue to pass against the unified writer-routed protocol.

  Next: the remaining two PR 2 cleanups (delete backup_service.py, drop
  restore_from_backup from reconciliation_service public exports) are
  gated on PR 3's removal of manual_transfer_resolution_service.py — the
  only forward-mutation caller of `backup_file` that is NOT a legacy
  redundant call inside a writer block. (The redundant calls in
  reconciliation_service.write_assertion_transaction and
  import_history_service.undo_import are harmless duplicate backups that
  get cleaned up alongside backup_service.py.)

2026-06-20 — PR 3 cleanup: removed the manual transfer resolution feature
  end-to-end and recorded DECISIONS §22. This unblocks the remaining two
  PR 2 cleanups (backup_service.py deletion + restore_from_backup export
  trim), which were gated on this feature's removal as the last forward
  caller of `backup_file` outside writer blocks.

  Files deleted in full:
    - app/backend/services/manual_transfer_resolution_service.py (340 lines)
    - app/backend/tests/test_manual_transfer_resolution_service.py (279 lines)
    - app/frontend/src/lib/components/transactions/ManualResolutionDialog.svelte (233 lines)

  Backend surface trimmed:
    - main.py: dropped both route handlers
      (/api/transactions/manual-transfer-resolution/preview, /apply),
      the `ManualTransferResolutionRequest` import, and the
      `apply_manual_transfer_resolution` / `preview_manual_transfer_resolution`
      imports. With the apply route gone, four event-log/backup helpers
      lost their last main.py consumer and were dropped from the imports:
      `backup_file` (from services.backup_service), and `check_drift`,
      `emit_event`, `hash_file` (from services.event_log_service).
      `rel_path` and `check_startup_drift` remain in active use.
    - models.py: dropped `ManualTransferResolutionRequest` pydantic class.
    - services/transfer_service.py: dropped the four manual-resolution
      constants (`MANUAL_TRANSFER_RESOLUTION_METADATA_KEY`,
      `MANUAL_TRANSFER_RESOLUTION_METADATA_VALUE`,
      `MANUAL_TRANSFER_RESOLUTION_TOKEN_KIND`,
      `MANUAL_TRANSFER_RESOLUTION_TOKEN_VERSION`) and both token helpers
      (`build_manual_transfer_resolution_token`,
      `parse_manual_transfer_resolution_token`). Because those helpers
      were the only consumers of `base64` and `json` in this module, both
      module-level imports were dropped. The `transfer_pair_account` /
      `parse_transfer_metadata` / `is_transfer_account` exports survive
      unchanged — used by `transaction_helpers` and elsewhere.
    - services/transaction_helpers.py: dropped the
      `MANUAL_TRANSFER_RESOLUTION_METADATA_KEY/VALUE` +
      `build_manual_transfer_resolution_token` imports from
      `transfer_service`. Dropped the `manual_resolution_token` and
      `manual_resolution_note` fields from the `RegisterEvent` dataclass.
      Dropped both module-level helper functions
      (`manual_resolution_token(config, transaction, *, grouped_settled)`,
      `manual_resolution_note(transaction)`). Removed the corresponding
      `token = manual_resolution_token(...)` + `manual_resolution_token=token,
      manual_resolution_note=manual_resolution_note(transaction)` lines from
      `pending_transfer_event_for_peer_account`.
    - services/account_register_service.py: dropped
      `manual_resolution_token` / `manual_resolution_note` imports, the
      `token = None` initializer + `token = manual_resolution_token(...)`
      branch in the per-transaction loop, and the
      `manual_resolution_token=token,
      manual_resolution_note=manual_resolution_note(transaction),`
      keyword args on the `RegisterEvent` constructor. Dropped the
      `manualResolutionToken` / `manualResolutionNote` keys from the
      per-entry response dict.
    - services/unified_transactions_service.py: same shape of cleanup as
      `account_register_service` — dropped the two helper imports, the
      `token = None` initializer + `token = manual_resolution_token(...)`
      branch, the two `RegisterEvent(...)` keyword args, and the two
      response-dict keys.

  Backend tests trimmed:
    - tests/test_account_register_service.py: dropped the
      `MANUAL_TRANSFER_RESOLUTION_METADATA_KEY/VALUE` imports from the
      `services.transfer_service` import block. Removed the two
      feature-specific tests
      (`test_account_register_exposes_same_manual_resolution_token_on_source_and_peer_pending_rows`,
      `test_account_register_adds_manual_resolution_note_for_resolved_transfer_rows`).
      Pruned the residual `manualResolutionToken is None` /
      `manualResolutionToken is not None` assertions from the surviving
      bilateral-match tests (the field no longer exists on the response
      shape). `transfer_pair_account` import remains — still used by
      every other transfer-related test in the file.

  Frontend surface trimmed:
    - routes/transactions/+page.svelte: dropped the `ManualResolutionDialog`
      import, the `ManualResolutionApplyResult` type import, the
      `manualResolutionEntry` / `manualResolutionSuccess` reactive
      variables, the `openManualRes` and `handleResolved` handlers, the
      `{#if manualResolutionSuccess}` "Resolved" success card, and the
      `<ManualResolutionDialog ... />` mount. In the pending-transfers
      list, the pending-details-note collapses from the
      `row.manualResolutionToken ? '…' : 'Waiting for the imported
      transaction to land.'` conditional down to the single
      "Waiting for the imported transaction to land." copy, and the
      `{#if row.manualResolutionToken}` "Resolve manually" affordance is
      gone. (Most of these edits were applied automatically by the
      editor's svelte-aware linter once the dialog import and entry-type
      reference disappeared; the manual deletions covered the dialog
      mount, the success card, and the pending-list button.)
    - lib/transactions/types.ts: dropped the deprecated `RegisterEntry`
      type (its only consumers were `ManualResolutionDialog` and the
      now-removed `manualResolutionEntry` binding in the transactions
      route), the `ManualResolutionPreview` type, the
      `ManualResolutionApplyResult` type, and the
      `manualResolutionToken` / `manualResolutionNote` fields on
      `TransactionRow`.
    - lib/components/transactions/TransactionDetailSheet.svelte: dropped
      the `{#if row.manualResolutionNote} <p>{row.manualResolutionNote}
      </p> {/if}` informational block that displayed the
      "missing-import-counterpart" note on resolved transfer rows. The
      `settled_grouped` informational block above it is untouched.

  DECISIONS.md: §22 already present (auto-added or pre-existing in the
  working tree at the time of cleanup; verified to match the PRD's
  required shape: states the decision, the load-bearing reason — writer
  requires `paths` up front and previous design required discover-after
  glob-all-journals path resolution incompatible with that contract,
  the constraint on any future reintroduction — pre-resolve target path
  outside the writer and be motivated by a real-world workflow — and
  links back to `plans/journal-writer-prd.md`). No edit needed.

  Tests: full backend suite: 796 passed, 0 failures (was 804; 8 fewer
  matches the deleted-test count — 6 from the deleted service test +
  the 2 manual-resolution tests pruned from
  `test_account_register_service.py`). Frontend type check
  (svelte-check): 870 files, 0 errors, 0 warnings.

  No grep hits remain in non-archived source for `manual_transfer_resolution`,
  `ManualTransferResolution`, `manual-transfer-resolution`,
  `MANUAL_TRANSFER_RESOLUTION_*`, `manualResolutionToken`,
  `manualResolutionNote`, `manual_resolution_token`,
  `manual_resolution_note`, `build_manual_transfer_resolution_token`,
  `parse_manual_transfer_resolution_token`, `openManualRes`,
  `handleResolved`, `RegisterEntry`, or `transfer_resolution.applied`.
  Remaining hits live only in `.svelte-kit/output/` (generated build
  artifacts; regenerated on next build), `worktrees/yolo/` (snapshot
  of stale work), `plans/transaction-identity-fix.md`,
  `plans/transactions-rethink.md`,
  `plans/transactions-decomposition.md` (historical planning docs),
  this progress log, the PRD, and the closed issue file
  `issues/003-remove-manual-transfer-resolution-and-record-decisions-22.md`.

  Next: with manual-transfer-resolution gone, the final two PR 2
  cleanups can land — delete `services/backup_service.py` and drop
  `restore_from_backup` from `services/reconciliation_service`'s
  public exports. `backup_file` still has a few in-tree call sites
  (`reconciliation_service.write_assertion_transaction` and
  `import_history_service.undo_import` each call it redundantly
  inside a writer block; a few backend tests still construct backups
  directly). `restore_from_backup` no longer has any production
  consumers (route rollback is handled by the writer); the only
  remaining reference is `test_reconciliation_service.py` exercising
  byte-identity round-trip — that test plus the public export can go.

- 2026-06-20 PR 2 sweep — convert `accounts_reconciliation_duplicate_resolution`
  route's three action branches through `journal_writer.mutate`. This
  route was added concurrently with the writer (commits `a1d69a1` …
  `ae06780` predated and chased PR 1 / `eb4268b`), so it was never
  enumerated in the original sweep list. The PRD's claim that the
  remaining `backup_file` calls were only inside writer blocks was
  wrong — `reconciliation_duplicate_service.resolve_duplicate_candidate`
  was still doing the full manual dance (capture-hash → backup →
  mutate → emit → manual-rollback try/except). Converting it is the
  unstated prerequisite for the backup_service.py deletion.

  Service file (`app/backend/services/reconciliation_duplicate_service.py`):

  * Imports trimmed:
    - dropped `.backup_service.backup_file`,
    - dropped `.event_log_service.check_drift`,
    - dropped `.event_log_service.emit_event`,
    - dropped `.event_log_service.hash_file`,
    - dropped `.archive_service.rollback_archive`,
    - retained `.event_log_service.rel_path` (still used in the
      `remove_manual_duplicate` payload to format `journal_path`),
    - added `from . import journal_writer`.

  * Dead helpers removed (each had exactly one production caller —
    `resolve_duplicate_candidate` — and that caller is now a writer
    block):
    - `_capture_hash_before(config, path, hashes)`
    - `_backup_once(path, tag, backups)`
    - `_emit_resolution_event(config, event_type, summary, payload,
      hash_before_by_path)`

  * `resolve_duplicate_candidate(config, tracked_account_id,
    checked_row, unchecked_row, action)` rewrites:

    * Pre-block validation kept outside `mutate`:
      - tracked account 404,
      - missing ledger_account 400,
      - merge_imported_duplicates: cross-journal 422 ("Imported
        duplicates must live in the same journal file to merge.").

    * Branch `remove_manual_duplicate`:
      paths=[journal_path], tag="reconcile-duplicate",
      event_type="reconciliation.duplicate_manual_removed.v1".
      `_read_block(target)` runs outside `mutate` (it raises a 409
      on header-relocate, which is a stale-read error rather than a
      mutation failure — pre-block keeps the writer from doing
      pointless backups). Block removal (with leading blank line
      consumption), `deleted_block` capture, and `_write_journal`
      happen inside the block. Payload + summary set inside the
      block. Return `mut.event_id`.

    * Branch `use_imported_transaction`:
      paths=[manual_journal, archive_path] when manual_journal ==
      imported_journal, else [manual_journal, imported_journal,
      archive_path]. tag="reconcile-duplicate",
      event_type="reconciliation.imported_transaction_used.v1".
      `_read_block(manual_row)` and `_read_block(imported_row)` run
      outside `mutate` (same 409-on-header-relocate reasoning as
      above; pre-block also lets us decide the `paths=` list based on
      whether the two journals are the same file). archive_path is
      always declared in `paths`, whether or not it pre-exists — the
      writer's `pre_existed`/`hash_before` machinery handles both
      cases (delete-if-newly-created on rollback, byte-restore from
      backup otherwise). File-content-dependent 422s ("Split manual
      duplicates need a fuller merge flow before they can replace an
      imported transaction", "Imported duplicate does not have a
      usable category posting") stay **inside** the block — they
      raise `HTTPException`, escape the block, and the writer rolls
      back manual_journal, imported_journal, and archive_path from
      backup or unlink. Mutation steps inside the block: extract
      manual/imported blocks (re-locating them in `shared_lines` when
      same-journal); compute destination_account from manual's other
      posting; rewrite imported's other-posting to destination_account
      when they differ; `_replace_user_metadata` to graft manual's
      user metadata onto imported; mint `match_id` (uuid4);
      `_upsert_match_tags`; `archive_manual_entry(archive_path,
      match_id, manual_block)`; splice updated_imported into shared
      or imported lines; remove manual block (with trailing blank
      line consumption); `_write_journal` once for shared lines or
      twice for distinct journals. Payload + summary set inside the
      block. Return `mut.event_id`.

    * Branch `merge_imported_duplicates`:
      paths=[journal_path], tag="reconcile-duplicate",
      event_type="reconciliation.imported_duplicates_merged.v1".
      `_read_block(survivor_row)` and `_read_block(merged_row)` plus
      the cross-journal 422 check run outside `mutate`. Inside the
      block: extract survivor / merged blocks; call
      `_upsert_import_identity_metadata` (which can raise 422 on
      missing import identity — escapes the block and writer rolls
      back); splice updated_survivor into lines; shift merge
      start/end indices when survivor precedes the merge; remove the
      merge block (with leading blank line consumption);
      `_write_journal`; conditionally call
      `ImportIndex(...).upsert_transactions(...)` to maintain the
      import index. Payload + summary set inside the block. Return
      `mut.event_id`. (ImportIndex writes are to .workflow/state.db,
      which is outside the writer's journal-class scope per
      PRD §6 — they ride along inside the block so a journal-write
      failure rolls back consistently with the index state. Note the
      index write itself is not rolled back by the writer; if the
      journal write succeeds but the ImportIndex call later fails,
      the journal is committed and the index remains stale, but the
      reverse failure mode — journal write failing first — is the
      common case and is handled correctly.)

  * Removed `try: ... except Exception: <manual rollback> raise`
    wrappers from all three branches. Removed
    `manual_original_text` / `imported_original_text` /
    `archive_size_before` captures (writer owns backups).
    Removed the `rollback_archive(archive_path,
    archive_size_before)` call in the exception path of
    `use_imported_transaction` — the writer's byte-restore (or
    delete-when-newly-created) of `archive_path` from its
    pre-mutation backup dominates the in-place truncate.

  Test file (`app/backend/tests/test_reconciliation_duplicate_review.py`):

  * Dropped `from services import reconciliation_duplicate_service`
    (only consumer was the removed test).
  * Removed `test_duplicate_resolution_rolls_back_when_event_write_fails`.
    Its assertion ("monkeypatch emit_event to raise, expect the
    journal text to be rolled back") is no longer part of the
    writer's contract — the writer calls `emit_event` *after* the
    `with` block exits successfully, so an emit failure propagates
    without rolling back, matching every other PR 2 conversion's
    "emission failures are now real errors" framing. The
    rollback-on-block-exception behavior is exhaustively covered by
    `test_journal_writer.py`; no coverage gap is introduced. Other
    8 tests in the file are untouched and still pass.

  Tests: full backend suite: 795 passed, 0 failures (was 796; the -1
  is the deleted rollback-on-emit test). No type check needed —
  the route's external response shape (`removedSelectionKeys`,
  `addedCheckedSelectionKeys`, `eventId`) is byte-identical to
  before.

  Behavioral notes:
  * `journal_refs` ordering and filtering is now governed by the
    writer (insertion-order of `paths=`, refs only when
    hash_before \!= hash_after). Previously the manual
    `_emit_resolution_event` sorted by `str(path)`. Test
    assertions on `events[-1]["type"]` are unaffected.
  * Event `actor` now defaults to "user" via the writer (previously
    `_emit_resolution_event` called `emit_event` without an actor
    so it took whatever default `emit_event` provides). Spot-check:
    same default ("user").
  * Event ids are now uuid7 (writer-minted) instead of whatever
    `emit_event` previously minted. uuid7 is the project-wide
    standard; no test asserts on uuid version.
  * Pre-block 409 ("Transaction moved since this page loaded.
    Refresh and try again.") from `_read_block` runs before the
    writer entry, so no backup is created on stale-read — minor
    win, matches the pattern used for the toggle-status route
    where `_locate_header` errors run before `mutate`.

  Next: now that all forward mutations flow through the writer, the
  final PR 2 cleanup can land — delete `services/backup_service.py`,
  drop `restore_from_backup` from `services/reconciliation_service`'s
  public exports, drop the duplicate `backup_file` calls inside
  `reconciliation_service.write_assertion_transaction` and
  `import_history_service.undo_import` (and the `undoBackupPath`
  field on the import-history undo entry — adjust
  `test_import_history_service.py` and any frontend that reads it),
  drop the `test_reconciliation_service.test_restore_from_backup_yields_byte_identical_journal`
  test plus the `restore_from_backup` import in that test file, and
  inline the trivial 4-line backup helper (or stop backing up) in
  the remaining test files that still construct backups directly
  (`test_undo_service.py`, `test_notes_and_recategorize.py`,
  `test_transaction_actions.py`).

[2026-06-20 — backup_service.py deletion]

  Closed the final PR 2 task: delete `backup_service.py` and drop
  `restore_from_backup` from `reconciliation_service`'s public exports.
  This was the last load-bearing cleanup gating PR 2's "writer is the
  only place backups happen" claim.

  Service deletions:
  * `app/backend/services/backup_service.py` removed (4-line module:
    `backup_file(path, tag)` → `Path` that timestamped + copied bytes).
  * `services/reconciliation_service.restore_from_backup` removed —
    4-line `shutil.copyfile(str(backup_path), str(journal_path))`
    wrapper. The `shutil` import dropped (sole consumer).
  * `services/reconciliation_service.write_assertion_transaction`
    signature simplified from `tuple[AssertionWriteResult, Path]` to
    just `AssertionWriteResult`. The function's inner
    `from .backup_service import backup_file` local import + the
    `backup_path = backup_file(journal_path, "reconcile")` call are
    gone — these were a duplicate of the writer's pre-mutation backup
    (the route wraps `write_assertion_transaction` in
    `journal_writer.mutate(paths=[target_journal_path], ...)`, so the
    writer captures hash_before, backs up the journal, then the inner
    call would back it up again — harmless until now, removed
    alongside the service deletion). Docstring updated to drop the
    "Returns ``(result, backup_path)``" line and the
    "caller is responsible for verifying ... and rolling back" note —
    the writer owns both.
  * `services/import_history_service.undo_import` drops
    `undo_backup = backup_file(journal_path, "undo") if
    journal_path.exists() else None` and the `undoBackupPath` field on
    the entry's `undo` dict. The `from .backup_service import
    backup_file` import dropped. This was a similar "duplicate backup
    inside the writer block" — the route
    `/api/imports/{id}/undo` wraps `undo_import` in
    `journal_writer.mutate(paths=[journal_path], ...)`, so the writer's
    backup dominates.

  Main.py route updated:
  * `accounts_reconcile`: `write_result, _backup =
    write_assertion_transaction(...)` → `write_result =
    write_assertion_transaction(...)`.

  Frontend update:
  * `app/frontend/src/lib/components/ImportFlow.svelte`: type
    declaration for the import-history entry's `undo` dict loses the
    `undoBackupPath?: string | null` field. (The field was never
    displayed — only typed.)

  Test updates:
  * `app/backend/tests/test_reconciliation_service.py`:
    - Dropped `restore_from_backup` from the `services.reconciliation_service`
      import block.
    - Removed `TestRollback.test_restore_from_backup_yields_byte_identical_journal`
      (24 lines). The rollback-via-backup behavior it asserted lives in
      the writer now and is covered by `test_journal_writer.py`.
    - Updated 3 `result, _ = write_assertion_transaction(...)` calls
      to `result = write_assertion_transaction(...)`. The other 3
      `write_assertion_transaction` calls in the file already
      discarded the result, so no tuple-unpack to update for those.
  * `app/backend/tests/test_undo_service.py`: dropped 5 inline
    `from services.backup_service import backup_file` + matching
    `backup_file(journal, "delete"|"recategorize"|"toggle")` calls
    in 5 tests (`test_already_compensated`, `test_drift_detected`,
    `test_round_trip_delete_and_restore`,
    `TestUndoRecategorized.test_round_trip_recategorize_and_restore`,
    `TestUndoStatusToggled.test_round_trip_toggle_and_restore`). The
    backups themselves were never read by any assertion — they were
    warm-up calls modeling the legacy dance but the tests assert
    against the event log, not the backup paths.
  * `app/backend/tests/test_notes_and_recategorize.py`: dropped the
    top-level `from services.backup_service import backup_file`
    import and 3 `backup_file(journal, "recategorize"|"notes")` calls
    in `TestRecategorizeWithNewCategory.test_sets_specific_category`,
    `TestRecategorizeWithNewCategory.test_without_new_category_resets_to_unknown`,
    and `TestSaveNotes.test_inserts_notes_line`. Same rationale —
    backups were never read.
  * `app/backend/tests/test_transaction_actions.py`: dropped 3 inline
    `from services.backup_service import backup_file` imports +
    matching `backup_file(...)` calls in
    `TestDeleteTransaction.test_removes_transaction_block`,
    `TestRecategorizeTransaction.test_rewrites_destination_to_unknown`,
    and `TestUnmatchTransaction.test_restores_manual_entry_and_removes_tags`
    (the unmatch test had two `backup_file` calls, one for each of
    main journal + archive).
  * `app/backend/tests/test_import_history_service.py`: dropped two
    `assert Path(undone["undo"]["undoBackupPath"]).exists()` /
    `.read_text(encoding="utf-8")` assertions on the
    `test_undo_import_removes_only_imported_transactions` test (since
    the `undoBackupPath` field no longer exists in the response).
  * `app/backend/tests/test_journal_writer.py`: docstring updated to
    refer to "the writer's internal backup helper" instead of the
    deleted `backup_file`.

  Tests: 794 passing (was 795 before this PR — the -1 is the deleted
  `test_restore_from_backup_yields_byte_identical_journal`). Frontend
  type check: 870 files, 0 errors, 0 warnings.

  Cross-cutting check: `grep -RIn 'backup_service\|backup_file\|
  restore_from_backup' app/backend app/frontend` after the edits
  returns zero references in production code and zero references in
  tests (excluding the documentation comment-mention in PRD / progress
  notes). The `backup_service.py` deletion landed cleanly: no
  consumer left, no test imports left.

  PR 2 sweep status: **complete**. Every forward and undo journal
  mutation in the app now flows through
  `journal_writer.mutate(paths=[...], tag=..., event_type=...)`. The
  legacy dance — `check_drift` + `backup_file` + `hash_file` +
  `emit_event` — no longer exists anywhere outside the writer's body.
  The PRD's leverage argument now holds end-to-end:
  one grep target (`with journal_writer.mutate(`) lists every
  mutation site, one module owns the seven-step ritual, and adding
  a new mutation costs ~10 lines at the call site instead of ~30.
