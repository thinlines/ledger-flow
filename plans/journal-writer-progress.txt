2026-06-20 — PR 1 task 1/3 complete: introduced services/journal_writer.py.

  Module: app/backend/services/journal_writer.py
    - mutate(*, config, paths, tag, event_type, verify=None, actor="user") context manager
    - JournalMutation(event_id, summary, payload, compensates) handle
    - VerifyFailure marker base class
    - WriterRejected / WriterUnavailable / WriterError exception surface
    - Inlined backup helper (backup_service.py to be deleted in PR 2)
    - Path validation: at least one *.journal path required

  Tests: app/backend/tests/test_journal_writer.py — 28 tests, all pass.
    Covers: event_id minting, hash_before consistency, backup creation,
    successful-exit event shape and journal_refs filtering (changed paths
    only, newly-created paths included), block-exception rollback (existing
    files restored byte-for-byte, created files deleted, no event emitted),
    verify→VerifyFailure rollback + WriterRejected, verify→RuntimeError
    rollback + WriterUnavailable, zero-journal-path rejection, partial
    rollback aggregation + original-exception preservation via __context__.

  Docs: CONTEXT.md created (lazy per PRD Further Notes) with journal-mutation
  definition + scope.

  Full backend suite: 804 passed (28 new + 776 existing, no regressions).

  Next: convert accounts_reconcile route to use mutate(verify=verify_assertion);
  then convert _undo_transaction_deleted handler.

2026-06-20 — PR 1 task 2/3 complete: converted accounts_reconcile route.

  Route: app/backend/main.py — accounts_reconcile
    - Wrapped journal write in `with journal_writer.mutate(paths=[year journal],
      tag="reconcile", event_type="account.reconciled.v1",
      verify=lambda cfg, _paths: verify_assertion(cfg)) as mut:`
    - Uses `mut.event_id` (writer-minted) to embed the reconciliation_event_id in
      journal metadata via write_assertion_transaction(event_id=mut.event_id),
      preserving DECISIONS §15 byte-identity between the in-journal metadata and
      the emitted event id.
    - Sets `mut.summary` + `mut.payload` inside the block — writer emits the
      event with assembled journal_refs on successful exit.
    - WriterRejected (verify_assertion → AssertionFailure) translates to
      HTTP 422 with outcome/expected/actual/rawError detail preserved.
    - WriterUnavailable (verify_assertion → RuntimeError) translates to
      HTTP 500 with the existing "ledger CLI is unavailable" copy.
    - write_assertion_transaction return tuple now discards `backup_path`
      (writer owns rollback). Inner backup still happens; harmless until PR 2
      sweeps backup_service.

  Adapter: reconciliation_service.AssertionFailure now subclasses
  journal_writer.VerifyFailure (marker). No callers needed changes; the
  inheritance just lets the writer carry it via WriterRejected.failure.

  Imports: main.py drops `restore_from_backup` (no remaining route uses it).
  reconciliation_service.py imports VerifyFailure for the marker base.

  Tests: 70 focused tests pass (test_reconcile_endpoint 13, test_reconciliation
  _service 29, test_journal_writer 28). No shape changes to existing tests.
  Full backend suite: 804 passed, 0 failures.

  Next: convert _undo_transaction_deleted handler in app/backend/services/
  undo_service.py to flow through mutate(..., compensates=original_event_id).

2026-06-20 — PR 1 task 3/3 complete: converted _undo_transaction_deleted handler.

  Handler: app/backend/services/undo_service.py — _undo_transaction_deleted
    - Signature changed from (workspace_path: Path, event: dict) -> dict[str, str]
      to (config: AppConfig, event: dict) -> str (returns the compensating
      event id).
    - Restoration logic wrapped in `with journal_writer.mutate(paths=[journal],
      tag="undo", event_type=f"{forward_type}.compensated.v1") as mut:`.
    - Sets `mut.summary`, `mut.payload` (forward payload + compensated_event_id),
      `mut.compensates = forward_event_id` inside the block — writer emits the
      compensating event with assembled journal_refs on successful exit.
    - Pre-block safety check (refuse if header already present) stays outside
      `mutate` so a duplicate-rejection neither backs up nor emits.
    - Legacy `backup_file(journal_path, "undo")` call dropped — writer owns
      backup + rollback now.

  Dispatcher: undo_event(config: AppConfig, event_id: str)
    - Signature changed from undo_event(workspace_path, event_id) — workspace
      is taken from config.root_dir internally.
    - New HandlerFn protocol union (LegacyHandlerFn | WriterHandlerFn);
      _WRITER_HANDLERS frozenset names event types whose handlers use the
      writer protocol. Discrimination via set lookup, not isinstance.
    - Legacy emit-on-success branch retained for the six remaining handlers
      until PR 2 sweeps them.
    - Per-event drift verification stays in undo_event (precondition before
      handler dispatch), per PRD Decision 5.

  Caller: main.py events_undo route now passes `config` instead of
  `config.root_dir`.

  Tests: app/backend/tests/test_undo_service.py
    - Added _make_config(workspace) helper (minimal AppConfig — root_dir +
      empty dicts).
    - All 16 call sites updated mechanically: undo_event(workspace, id) →
      undo_event(_make_config(workspace), id). No assertion shape changes.
    - 15 tests pass (one is parametrized count). Round-trip restore test
      still asserts journal text restoration; already-compensated test still
      asserts r1.compensating_event_id == r2.compensating_event_id, now
      verifying that the writer-emitted event id is what _is_compensated
      finds.

  Full backend suite: 804 passed, 0 failures.

  PR 1 (proof) is now complete. Next: PR 2 (sweep) — convert remaining ~14
  mutation routes and six undo handlers, delete backup_service.py, remove
  restore_from_backup from the event-log service's public exports, drop
  the legacy-handler branch from undo_event.

2026-06-20 — PR 2 sweep step 1: converted _undo_transaction_recategorized.

  Handler: app/backend/services/undo_service.py — _undo_transaction_recategorized
    - Signature changed from (workspace_path: Path, event: dict) -> dict[str, str]
      to (config: AppConfig, event: dict) -> str (returns the compensating event id).
    - Body wrapped in `with journal_writer.mutate(paths=[journal_path], tag="undo",
      event_type=f"{forward_type}.compensated.v1") as mut:`. Header lookup,
      block lookup, posting rewrite, and journal write all happen inside the
      block — if the destination account is not found the handler raises
      UndoFailedError inside the block and the writer rolls back (no-op since
      the in-memory `lines` mutation never reached disk, but backup is still
      restored harmlessly).
    - Legacy `backup_file(journal_path, "undo")` call dropped — writer owns
      backup + rollback now.
    - Sets `mut.summary` (f"Undid: {forward_summary}"), `mut.payload`
      (forward payload + compensated_event_id), `mut.compensates =
      forward_event_id` inside the block. Writer emits the compensating event
      with assembled journal_refs on successful exit.

  Dispatcher: _WRITER_HANDLERS now contains both transaction.deleted.v1 and
  transaction.recategorized.v1. No further dispatcher changes — the existing
  set-lookup discrimination handles the new entry transparently.

  Tests: test_undo_service.TestUndoRecategorized.test_round_trip_recategorize_
  and_restore passes unchanged. Full undo + journal_writer focused: 43 tests.
  Full backend suite: 804 passed, 0 failures.

  Next: convert _undo_transaction_account_reassigned (structurally identical
  to recategorized — same posting-rewrite pattern).

2026-06-20 — PR 2 sweep step 2: converted _undo_transaction_account_reassigned.

  Handler: app/backend/services/undo_service.py — _undo_transaction_account_reassigned
    - Signature changed from (workspace_path: Path, event: dict) -> dict[str, str]
      to (config: AppConfig, event: dict) -> str.
    - Body wrapped in `with journal_writer.mutate(paths=[journal_path], tag="undo",
      event_type=f"{forward_type}.compensated.v1") as mut:`. Read, header lookup,
      block lookup, posting rewrite, and journal write all happen inside the
      block; UndoFailedError raised inside the block triggers writer rollback.
    - Legacy `backup_file(journal_path, "undo")` call dropped — writer owns
      backup + rollback now.
    - Sets `mut.summary` (f"Undid: {forward_summary}"), `mut.payload`
      (forward payload + compensated_event_id), `mut.compensates =
      forward_event_id` inside the block. Writer emits the compensating event
      with assembled journal_refs on successful exit.

  Dispatcher: _WRITER_HANDLERS now contains transaction.deleted.v1,
  transaction.recategorized.v1, and transaction.account_reassigned.v1. No
  further dispatcher changes — set-lookup discrimination handles the new entry
  transparently.

  Tests: No existing tests directly cover this handler; the dispatcher path
  exercises it via _HANDLERS lookup + writer-routed protocol. Full undo +
  journal_writer focused: 43 tests pass. Full backend suite: 804 passed,
  0 failures.

  Next: convert _undo_transaction_status_toggled.

2026-06-20 — PR 2 sweep step 3: converted _undo_transaction_status_toggled.

  Handler: app/backend/services/undo_service.py — _undo_transaction_status_toggled
    - Signature changed from (workspace_path: Path, event: dict) -> dict[str, str]
      to (config: AppConfig, event: dict) -> str.
    - Body wrapped in `with journal_writer.mutate(paths=[journal_path], tag="undo",
      event_type=f"{forward_type}.compensated.v1") as mut:`. Read, new_status
      validation, current-header reconstruction (set_header_status(header_line,
      new_status)), header locate, previous_status validation, status restore,
      and journal write all happen inside the block. UndoFailedError raised
      inside the block triggers writer rollback.
    - Legacy `backup_file(journal_path, "undo")` call dropped — writer owns
      backup + rollback now.
    - Sets `mut.summary` (f"Undid: {forward_summary}"), `mut.payload`
      (forward payload + compensated_event_id), `mut.compensates =
      forward_event_id` inside the block. Writer emits the compensating event
      with assembled journal_refs on successful exit.

  Dispatcher: _WRITER_HANDLERS now contains transaction.deleted.v1,
  transaction.recategorized.v1, transaction.account_reassigned.v1, and
  transaction.status_toggled.v1. No further dispatcher changes — set-lookup
  discrimination handles the new entry transparently.

  Tests: test_undo_service.TestUndoStatusToggled.test_round_trip_toggle_and_
  restore passes unchanged. Full undo + journal_writer focused: 43 tests pass.
  Full backend suite: 804 passed, 0 failures.

  Next: convert _undo_manual_entry_created.

2026-06-20 — PR 2 sweep step 4: converted _undo_manual_entry_created.

  Handler: app/backend/services/undo_service.py — _undo_manual_entry_created
    - Signature changed from (workspace_path: Path, event: dict) -> dict[str, str]
      to (config: AppConfig, event: dict) -> str.
    - Body wrapped in `with journal_writer.mutate(paths=[journal_path], tag="undo",
      event_type=f"{forward_type}.compensated.v1") as mut:`. Read, header build
      (date + payee → "YYYY-MM-DD payee"), header locate, block locate, blank-line
      consume, block removal, and journal write all happen inside the block.
      UndoFailedError raised inside the block (e.g. header not located) triggers
      writer rollback.
    - Pre-block validation (missing date/payee, missing or empty journal_refs)
      stays outside `mutate` so an incomplete-payload rejection neither backs up
      nor emits.
    - Legacy `backup_file(journal_path, "undo")` call dropped — writer owns
      backup + rollback now.
    - Sets `mut.summary` (f"Undid: {forward_summary}"), `mut.payload`
      (forward payload + compensated_event_id), `mut.compensates =
      forward_event_id` inside the block. Writer emits the compensating event
      with assembled journal_refs on successful exit.

  Dispatcher: _WRITER_HANDLERS now contains transaction.deleted.v1,
  transaction.recategorized.v1, transaction.account_reassigned.v1,
  transaction.status_toggled.v1, and manual_entry.created.v1. No further
  dispatcher changes — set-lookup discrimination handles the new entry
  transparently.

  Tests: test_undo_service.TestUndoManualEntryCreated.test_round_trip_create_
  and_delete passes unchanged. Full undo + journal_writer focused: 43 tests
  pass. Full backend suite: 804 passed, 0 failures.

  Next: convert _undo_transaction_notes_updated.

2026-06-20 — PR 2 sweep step 5: converted _undo_transaction_notes_updated.

  Handler: app/backend/services/undo_service.py — _undo_transaction_notes_updated
    - Signature changed from (workspace_path: Path, event: dict) -> dict[str, str]
      to (config: AppConfig, event: dict) -> str.
    - Body wrapped in `with journal_writer.mutate(paths=[journal_path], tag="undo",
      event_type=f"{forward_type}.compensated.v1") as mut:`. Read, header locate,
      block locate, current-notes-line search, set/insert/remove of the `; notes:`
      line, and journal write all happen inside the block. UndoFailedError raised
      inside the block (header not located / ambiguous) triggers writer rollback.
    - Pre-block validation stays outside `mutate`:
        * pre-migration event missing `previous_notes` key — fails closed before
          backup or emission.
        * empty `journal_path` or `header_line` — rejected before backup.
      Payload-shape rejection neither backs up nor emits a compensating event.
    - Legacy `backup_file(journal_path, "undo")` call dropped — writer owns
      backup + rollback now.
    - Sets `mut.summary` (f"Undid: {forward_summary}"), `mut.payload`
      (forward payload + compensated_event_id), `mut.compensates =
      forward_event_id` inside the block. Writer emits the compensating event
      with assembled journal_refs on successful exit.

  Dispatcher: _WRITER_HANDLERS now contains transaction.deleted.v1,
  transaction.recategorized.v1, transaction.account_reassigned.v1,
  transaction.status_toggled.v1, manual_entry.created.v1, and
  transaction.notes_updated.v1. No further dispatcher changes — set-lookup
  discrimination handles the new entry transparently.

  Tests: test_undo_service.TestUndoNotesUpdated suite (4 tests: round-trip set
  + remove, round-trip replace, round-trip restore after deletion, pre-migration
  fail-closed) passes unchanged. Full undo + journal_writer focused: 43 tests
  pass. Full backend suite: 804 passed, 0 failures.

  Next: convert _undo_transaction_unmatched (multi-file: main journal +
  archive — only multi-path undo handler remaining).

2026-06-20 — PR 2 sweep step 6: converted _undo_transaction_unmatched (multi-file).

  Handler: app/backend/services/undo_service.py — _undo_transaction_unmatched
    - Signature changed from (workspace_path: Path, event: dict) -> dict[str, str]
      to (config: AppConfig, event: dict) -> str. First multi-path undo handler
      converted — pressure-tests the writer's `paths=[journal, archive]` contract
      under conditional pre-existence (archive is absent in the post-unmatch
      state because the original unmatch removed its only entry).
    - Body wrapped in `with journal_writer.mutate(paths=[journal_path,
      archive_path], tag="undo", event_type=f"{forward_type}.compensated.v1") as
      mut:`. Inside the block: re-stamp the imported transaction in the main
      journal (insert :manual: + match-id: tags after the header, rewrite
      destination posting from Expenses:Unknown to the original category),
      locate + remove the restored manual entry (with preceding blank-line
      consume), write the journal, then call archive_manual_entry to re-create
      the archive entry. UndoFailedError raised inside the block (header not
      located on either pass) triggers writer rollback of both files — the
      main journal restores from its backup, the archive is deleted if the
      block had created it.
    - Pre-block validation stays outside `mutate`:
        * missing journal_path / header_line / match_id / restored_manual_block
          — incomplete payload, rejected before backup or emission.
        * no category recoverable from restored_manual_block (heuristic:
          Expenses:/Income:/Revenue:) — rejected before backup or emission.
    - Legacy `backup_file(journal_path, "undo")` and
      `backup_file(archive_path, "undo")` calls dropped — writer owns backup +
      rollback for both files now.
    - Sets `mut.summary` (f"Undid: {forward_summary}"), `mut.payload`
      (forward payload + compensated_event_id), `mut.compensates =
      forward_event_id` inside the block. Writer emits the compensating event
      with assembled journal_refs (both paths included when changed) on
      successful exit.

  Behavioral note: the legacy dispatcher filtered the compensating event's
  journal_refs to entries also present in forward.journal_refs (which for
  unmatch held only the main journal). The writer instead iterates the declared
  `paths` and includes every path whose hash changed — so the compensating
  event now carries both the journal_path and archive_path refs (the latter
  with hash_before "sha256:none" since the archive was absent pre-mutation).
  This is more accurate; the test_compensating_event_emitted test passes
  unchanged because it only asserts existence + type.

  Dispatcher: _WRITER_HANDLERS now contains transaction.deleted.v1,
  transaction.recategorized.v1, transaction.account_reassigned.v1,
  transaction.status_toggled.v1, manual_entry.created.v1,
  transaction.notes_updated.v1, and transaction.unmatched.v1. Every undo
  handler now flows through the writer protocol — the legacy emit-on-success
  branch in undo_event is dead code once PR 2 sweeps the route surface, but
  it will be retained until the last forward route conversion lands (so the
  diff that drops the branch is contiguous with the conversions).

  Imports: dropped unused `backup_file` (from services.backup_service) and
  `rel_path` (from services.event_log_service) imports from undo_service.py
  — neither has any remaining caller in this module.

  Tests: TestUndoUnmatched (2 tests: round-trip unmatch+rematch, compensating
  event emitted) passes unchanged. Full undo + journal_writer focused: 43
  tests pass. Full backend suite: 804 passed, 0 failures.

  Next: sweep the remaining ~14 mutation routes in main.py. After that, drop
  backup_service.py, drop restore_from_backup from event_log_service exports,
  and drop the legacy-handler branch from undo_event.
